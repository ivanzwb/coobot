# BiosBot 数据库文档

## 1. 文档目的

本文档定义 BiosBot P0 阶段的持久化实体、表结构、关系、索引和状态约束。
本文档主要支撑 05-详细设计 中的模块落地，并为接口实现、事件存储、查询优化和数据维护提供持久化依据。
本文档重点回答“哪些信息需要落库、以什么表和字段保存、如何建立关系与索引”；这里的实体和字段均以持久化为目的，不展开模块内部运行时对象和处理流程。

## 2. 设计原则

- 任务是主聚合根。
- 会话历史、任务历史、知识库资料三者边界明确。
- 主表承载当前聚合状态，明细表承载过程明细，事件表承载回放事实源。

## 3. 核心持久化实体

- `Conversation`
- `Message`
- `Task`
- `TaskStep`
- `Attachment`
- `TaskOutput`
- `KnowledgeDocument`
- `MemoryEntry`
- `TaskWriteCommand`
- `PermissionRequest`
- `TaskEvent`
- `ModelCallLog`
- `AttachmentEvent`
- `Agent`
- `Skill`
- `PermissionPolicy`
- `SystemConfig`

## 4. 数据表分层

### 4.1 主表

- `conversations`
- `tasks`
- `attachments`
- `task_outputs`
- `knowledge_base_documents`
- `memory_entries`
- `task_write_commands`
- `permission_requests`
- `agents`
- `skills`
- `permission_policies`
- `system_configs`

### 4.2 明细表

- `messages`
- `task_steps`
- `task_input_slices`
- `task_output_refs`
- `agent_participations`
- `task_memory_links`
- `permission_decision_logs`
- `model_call_logs`

### 4.3 事件表

- `task_events`
- `attachment_events`
- `tool_invocation_logs`
- `knowledge_hit_logs`
- `attachment_events`

### 4.4 审计日志表

- `audit_logs`

## 5. 关系说明

```text
conversations 1 --- n messages
conversations 1 --- n attachments
conversations 1 --- n tasks
tasks         1 --- n task_steps
tasks         1 --- n task_outputs
tasks         1 --- n task_events
tasks         1 --- n task_memory_links
tasks         1 --- n task_write_commands
tasks         1 --- n permission_requests
tasks         1 --- n model_call_logs
attachments   1 --- n task_steps
attachments   1 --- 0..1 knowledge_base_documents
memory_entries 1 --- n task_memory_links
permission_requests 1 --- n permission_decision_logs
agents        1 --- n skills
agents        1 --- n permission_policies
agents        1 --- n memory_entries
skills        1 --- n permission_policies
```

## 6. 核心表结构

### 6.1 conversations（会话表）

本表主要承接默认单一会话窗口上下文与最近活跃接入端事实，不承担显式"新建会话来源"或多会话切换主流程语义；在单用户场景下默认只维护唯一上下文记录；来源：N-01.3、N-01.4、N-14。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：conv_xxx |
| `title` | string | 否 | 会话标题，用于展示和搜索，默认由系统根据首条消息生成 |
| `status` | enum | 是 | 会话状态：active（活跃）、archived（归档）、deleted（已删除） |
| `last_active_client_id` | string | 否 | 最近一次写入本会话主交互消息的接入端标识，用于追踪多端活跃状态 |
| `latest_task_id` | string | 否 | 最近一次关联的任务ID，用于会话上下文快速引用 |
| `created_at` | datetime | 是 | 会话创建时间，ISO8601格式 |
| `updated_at` | datetime | 是 | 会话最后更新时间 |
| `last_message_at` | datetime | 否 | 最后一条消息的发送时间 |

### 6.2 messages（消息表）

本表主要承接默认会话窗口中的输入消息、来源端标识与跨端显示范围事实，保证消息展示与同步策略可按发起端稳定追踪；来源：N-01.1、N-01.4、N-14。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：msg_xxx |
| `conversation_id` | string | 是 | 外键，关联conversations.id |
| `task_id` | string | 否 | 关联的任务ID，若此消息触发任务创建则填写 |
| `entry_point` | string | 是 | 消息来源入口，如 web、cli、api、websocket 等 |
| `origin_client_id` | string | 否 | 消息发起端唯一标识，用于多端区分和同步策略 |
| `sync_policy` | string | 是 | 同步策略：origin_only（仅发起端）、synced_clients（已同步端）、all_clients（所有端） |
| `visible_client_ids` | json | 否 | 允许显示此消息的客户端ID数组，默认为空表示遵循sync_policy |
| `role` | enum | 是 | 消息角色：user（用户）、assistant（助手）、system（系统） |
| `content` | text | 是 | 消息正文内容，支持Markdown格式 |
| `metadata` | json | 否 | 扩展元数据，如引用文件列表、快捷指令等 |
| `created_at` | datetime | 是 | 消息创建时间 |

### 6.3 attachments（附件表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：att_xxx |
| `conversation_id` | string | 否 | 外键，关联conversations.id |
| `task_id` | string | 否 | 外键，关联tasks.id，若附件关联到特定任务 |
| `message_id` | string | 否 | 外键，关联messages.id，若附件由特定消息上传 |
| `file_name` | string | 是 | 原始文件名 |
| `mime_type` | string | 是 | 文件MIME类型，如 application/pdf、image/png 等 |
| `file_size` | bigint | 是 | 文件大小（字节） |
| `page_count` | int | 否 | 文档页数，仅PDF/Word等文档类文件有效 |
| `storage_path` | string | 是 | 文件存储路径，如 /attachments/2026/03/xxx.pdf |
| `upload_status` | enum | 是 | 上传状态：uploading、completed、failed |
| `parse_status` | enum | 是 | 解析状态：queued（待解析）、parsing（解析中）、parsed（已解析）、failed（解析失败）、skipped（跳过） |
| `parse_method` | string | 否 | 解析方法：ocr（文字识别）、pdf_parser（PDF解析）、doc_parser（Word解析）等 |
| `parse_summary` | text | 否 | 解析结果摘要，提取的关键信息 |
| `parse_confidence` | float | 否 | 解析置信度，0-1之间 |
| `persisted_to_knowledge_base` | boolean | 是 | 是否已沉淀到知识库，默认false |
| `created_at` | datetime | 是 | 上传时间 |
| `parsed_at` | datetime | 否 | 解析完成时间 |

### 6.4 tasks（任务表）

本表是任务主链路的核心落库实体，承接入口判定、等待态重评估、父子任务安排、双阶段通知、降级交付、补充更新、多端显示范围与权限/写队列控制等状态事实；来源：N-01.1、N-01.2、N-01.4、N-01.5、N-02.1、N-02.2、N-02.4、N-02.5、N-03.1、N-03.2、N-03.3、N-04.1、N-04.2、N-04.3、N-04.4、N-07.2、N-07.3、N-07.5、N-09.2、N-09.4、N-09.5、N-13、N-14。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：task_xxx |
| `conversation_id` | string | 否 | 外键，关联conversations.id |
| `status` | enum | 是 | 任务状态：pending（待处理）、clarification_pending（待澄清）、planning（规划中）、arranged（已安排）、running（执行中）、completed（已完成）、partial_failed（部分失败）、failed（失败）、cancelled（已取消）、timeout（超时）、intervention_required（需人工介入） |
| `trigger_mode` | enum | 是 | 入口模式：immediate（即时执行）、queued（排队等待）、scheduled（定时执行）、event_triggered（事件触发） |
| `trigger_status` | enum | 否 | 触发状态：ready（就绪）、queued（排队中）、waiting_schedule（等待定时）、waiting_event（等待事件）、triggered（已触发） |
| `scheduled_at` | datetime | 否 | 定时任务的计划执行时间 |
| `trigger_rule` | json | 否 | 事件触发规则，包含eventType、filters等配置 |
| `trigger_decision_summary` | text | 否 | 入口判定摘要，解释为何识别为当前触发模式 |
- `trigger_decision_summary`：任务入口判定摘要，用于解释系统为何将该任务识别为当前触发模式。
- `complexity_decision_summary`：可选，复杂任务判定摘要，用于解释系统为何将当前请求识别为需要拆分为父任务与多个 Domain 子任务。
- `clarification_required_fields`：可选，JSON 数组；当任务因低置信度入口判定进入正式待澄清状态时，记录仍需用户补充或系统确认的关键字段。
- `clarification_resolution_summary`：可选，低置信度入口在原 Task 上完成澄清、采用安全默认路径或完成确认后的处理摘要。
- `clarification_closed_reason`：可选，若用户放弃继续澄清或系统将待澄清任务关闭，则记录关闭原因。
- `next_trigger_check_at`：可选，下一次入口等待态重评估时间。
- `intake_input_summary`：任务创建时冻结的入口输入摘要，用于保证延迟任务在释放执行时仍可解释原始入口语义。
- `entry_point`：任务对应输入来源入口。
- `origin_client_id`：任务对应主交互发起端标识。
- `sync_policy`：任务相关输入、进度、结果和权限授权的默认跨端同步策略。
- `visible_client_ids`：可选，JSON 数组；任务相关消息默认允许显示的接入端范围。
- `display_scope`：任务当前展示范围语义，如 `origin_only`、`synced_clients`、`all_clients`。
- `interaction_dedup_key`：可选，同一权限确认、确认执行或人工介入处置事实的跨端去重键；用于保证同一事实只产生一次最终有效交互结果。
- `interaction_decision_status`：可选，同一事实当前的交互处理状态，如 `pending`、`resolved`、`rejected_as_duplicate`。
- `interaction_resolved_at`：可选，该一次生效型交互最终被处理的时间。
- `fact_summary`：可选，JSON 对象；承载跨端处理同一事实前所需的最小事实摘要，至少包含任务标识、当前状态、触发原因、影响范围和待确认动作。
- `queue_position`：可选，任务在本地等待队列中的顺位快照。
- `parent_task_id`：可选，父任务 ID；用于把 Domain 子任务关联回 Leader 父任务。
- `assigned_domain_agent_id`：可选，当前子任务所属 Domain Agent。
- `arrangement_status`：可选，父任务或子任务的安排状态；允许值和语义以 05-详细设计 的状态枚举词表基线为准。该字段只表达安排链路状态，不承载最终通知阶段。
- `arrangement_summary`：可选，父任务对全部子任务排队/触发安排的聚合摘要。
- `estimated_completion_at`：可选，预计完成时间。
- `estimated_duration_minutes`：可选，预计总耗时分钟数。
- `user_notification_stage`：可选，面向用户的通知阶段；允许值和语义以 05-详细设计 的状态枚举词表基线为准。
- `waiting_anomaly_code`：可选，等待异常码，如 `WAIT_TIMEOUT`、`TRIGGER_INVALIDATED`、`SNAPSHOT_INVALIDATED`。
- `waiting_anomaly_summary`：可选，等待异常摘要，用于向界面和接口解释当前受阻原因。
- `last_trigger_evaluation_summary`：可选，最近一次入口重评估摘要。
- `waiting_threshold_mode`：可选，当前命中的等待阈值所属模式，如 `queued`、`scheduled`、`event_triggered`。
- `waiting_threshold_basis_summary`：可选，当前命中的等待阈值依据摘要，用于说明阈值来源、命中条件和当前判断结果。
- `intervention_required_reason`：可选，进入人工介入状态的直接原因。
- `current_step_id`：当前步骤。
- `assigned_leader_agent_id`：Leader Agent。
- `route_decision_summary`：路由决策摘要。
- `current_reasoning_summary`：可选，当前或最近一轮 Agent 执行回合的简短推理摘要；仅用于展示和审计，不保存完整原始思维链。
- `last_observation_summary`：可选，最近一次被后续决策消费的工具/检索/环境观察摘要。
- `next_action_summary`：可选，基于最近观察结果形成的下一步动作意图摘要。
- `plan_revision`：默认 1；表示当前父任务已落库的最新编排版本号。Leader 基于新的观察结果完成一次有效增量重编排时递增，用于稳定表达同一用户请求下的多轮编排事实。
- `replan_count`：默认 0；记录当前父任务已发生的增量重编排次数。
- `latest_plan_summary`：可选，最近一次正式 TaskPlan 或增量重编排后的计划摘要。
- `goal_satisfaction_status`：可选，最近一次目标满足性判断结果，如 `unknown`、`unsatisfied`、`satisfied`、`manual_intervention_required`。
- `goal_satisfaction_summary`：可选，最近一次目标满足性判断摘要，用于说明当前为何继续重编排、结束执行或转入人工介入。
- `selected_agent_ids`：本次任务选中的 Agent 列表，JSON 数组。
- `selected_skill_ids`：本次任务激活的 Skill 列表，JSON 数组。
- `memory_scope`：记忆加载范围，如 `conversation`、`history`、`selected`。
- `selected_memory_entry_ids_snapshot`：可选，创建时冻结的显式记忆条目 ID 集合，JSON 数组。
- `invalid_memory_entry_ids`：可选，释放执行前判定为失效的冻结记忆条目 ID 集合，JSON 数组。
- `snapshot_resolution`：可选，冻结快照失效处理结果，如 `unchanged`、`degraded_continue`、`manual_intervention_required`、`terminated`。
- `memory_load_summary`：任务启动时的记忆加载摘要。
- `memory_write_summary`：任务结束后的记忆写回摘要。
- `aggregate_version`：任务聚合版本号，用于任务级乐观锁和过期写入校验。
- `pending_write_command_count`：当前待消费任务写命令数量。
- `last_accepted_write_command_id`：最近一次被任务聚合接受的写命令 ID。
- `last_event_sequence`：当前任务已落库的最后事件序号，用于时间线排序和增量恢复。
- `last_event_cursor`：当前任务最近一次事件游标快照，用于查询接口和订阅恢复，并用于生成接口层的 `nextEventCursor`。
- `permission_status`：当前任务权限状态，如 `not_required`、`pending`、`approved`、`denied`。
- `permission_summary`：任务内权限决策摘要。
- `permission_decision_trace`：任务级权限判定摘要，记录最近一次高风险动作的权限判定轨迹摘要。
- `error_code` / `error_message`。
- `retryable`。
- `retry_count`：整任务累计重试次数。
- `last_retry_source`：最近一次重试来源，如 `auto`、`manual`。
- `degrade_reason` / `degrade_action`：最近一次降级原因与处理动作。
- `model_provider`：最近一次成功或失败的模型提供方标识。
- `model_failure_code`：最近一次归一化后的模型失败码。
- `model_failure_summary`：最近一次模型失败摘要。
- `created_at` / `started_at` / `completed_at` / `total_duration_ms`。

### 6.5 task_steps（任务步骤表）

本表需承接步骤级 ReAct 执行摘要，用于表达某一步内的简短推理、实际动作与工具观察结果，而不是保存完整原始思维链；来源：N-03.1、N-07.5、N-12、N-13、N-14。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：step_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `attachment_id` | string | 否 | 外键，关联attachments.id，若此步骤涉及附件处理 |
| `agent_id` | string | 否 | 执行此步骤的Agent ID |
| `type` | enum | 是 | 步骤类型：intent_analysis（意图分析）、attachment_parse（附件解析）、knowledge_retrieval（知识检索）、agent_execution（Agent执行）、tool_invocation（工具调用）、result_synthesis（结果合成） |
| `name` | string | 是 | 步骤名称，如 "解析用户意图"、"调用代码执行工具" |
| `status` | enum | 是 | 步骤状态：pending（待执行）、running（执行中）、waiting（等待中）、completed（已完成）、failed（失败）、skipped（已跳过） |
| `parallel_group_id` | string | 否 | 并行步骤组ID，同一组内的步骤可并行执行 |
| `execution_mode` | enum | 是 | 执行模式：serial（串行）、parallel（并行）、waiting（等待） |
| `waiting_reason` | string | 否 | 进入等待状态的原因，如 waiting_permission（等待权限）、waiting_resource（等待资源）、waiting_trigger（等待触发） |
| `reasoning_summary` | text | 否 | 当前或最后一轮的简短推理摘要，仅用于展示和审计 |
| `action_summary` | text | 否 | 执行的动作摘要，如 "调用 Skill:CodeExecution，参数: {filename: test.py}" |
| `observation_summary` | text | 否 | 获得并被后续使用的观察结果摘要 |
| `observation_source_type` | enum | 否 | 观察来源类型：tool_result（工具结果）、knowledge_hit（知识命中）、model_output（模型输出）、environment（环境反馈） |
| `observation_ref` | string | 否 | 观察来源引用，如工具调用日志ID、知识命中文档ID |
| `input_summary` | text | 否 | 步骤输入摘要 |
| `output_summary` | text | 否 | 步骤输出摘要 |
| `error_message` | text | 否 | 错误信息，若步骤执行失败 |
| `retry_count` | int | 是 | 重试次数，默认0 |
| `started_at` | datetime | 否 | 步骤开始时间 |
| `completed_at` | datetime | 否 | 步骤完成时间 |
| `duration_ms` | bigint | 否 | 执行耗时（毫秒） |
| `created_at` | datetime | 是 | 步骤创建时间 |

### 6.6 task_outputs（任务输出表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：output_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `type` | enum | 是 | 输出类型：final（最终输出）、intermediate（中间结果）、arrangement（安排通知）、supplemental（补充更新） |
| `title` | string | 是 | 输出标题 |
| `content` | text | 是 | 输出内容，支持Markdown格式 |
| `format` | enum | 是 | 输出格式：text、markdown、json、html、file |
| `is_primary` | boolean | 是 | 是否为主输出，一个任务只能有一个主输出 |
| `output_stage` | enum | 是 | 输出阶段：arranged_only（仅安排通知）、final（最终交付）、supplemental（补充更新） |
| `agent_execution_summary` | json | 否 | Agent执行摘要，包含参与的Agent列表、执行轮次、关键决策 |
| `knowledge_hit_summary` | json | 否 | 知识命中摘要，包含检索查询、命中文档列表、相关性分数 |
| `memory_changes` | json | 否 | 本次任务新增或更新的记忆条目数组 |
| `model_call_summary` | json | 否 | 模型调用汇总，包含调用次数、token使用量、失败次数 |
- `memory_changes`：本次任务新增或更新的记忆条目摘要，JSON 数组。
- `model_call_summary`：模型调用汇总，JSON 对象。
- `reasoning_summary`：可选，结果页可展示的关键推理摘要；用于解释输出如何形成，但不承载完整原始思维链。
- `observation_summary`：可选，结果页可展示的关键观察摘要；用于解释哪些工具观察、检索结果或环境反馈支撑了当前输出。
- `react_loop_summary`：可选，JSON 对象；用于聚合记录关键 ReAct 回合中"推理 -> 动作 -> 观察 -> 决策变化"的压缩摘要。
- `normalized_failure_codes`：本次任务出现的归一化失败码列表，JSON 数组。
- `arrangement_notice_summary`：可选，阶段性 ETA / 安排通知摘要，JSON 对象。
- `degraded_delivery_summary`：可选，结构化降级交付判定摘要，JSON 对象；至少包含 `completed_blocking_task_ids`、`failed_or_skipped_non_blocking_task_ids`、`impact_summary`、`delivery_reason`。
- `supplemental_updates`：可选，交付后补充更新记录，JSON 数组；用于保存晚到 non-blocking 成功结果的到达时间、来源子任务、补充说明和影响范围。该字段只追加补充事实，不覆盖原最终输出内容。
- `supplemental_notification_type`：可选，补充更新通知类型，文本；用于标记本次补充更新对应的通知语义，如结果补充、影响说明或仅时间线更新。该字段只用于描述补充更新通知，不得复用为新的最终完成通知。
- `terminal_summary`：可选，终态摘要，JSON 对象；当任务合法结束但不存在可展示主输出物时必须写入，至少包含 `terminal_reason`、`completion_boundary`、`no_output_reason`、`recommended_next_actions`。
- `available_actions`：结果页可执行动作，如加入知识库、加入记忆，JSON 数组。
- `created_at`。

### 6.7 knowledge_base_documents（知识库文档表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：kbdoc_xxx |
| `agent_id` | string | 是 | 外键，关联agents.id，知识库所属Agent |
| `source_attachment_id` | string | 否 | 外键，关联attachments.id，文档来源附件 |
| `title` | string | 是 | 文档标题 |
| `content` | text | 是 | 文档内容，支持Markdown格式 |
| `source_type` | enum | 是 | 来源类型：manual（手动上传）、task_output（任务输出）、attachment_digest（附件摘要） |
| `metadata` | json | 否 | 扩展元数据，如标签、分类、来源任务ID等 |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

### 6.8 memory_entries（记忆条目表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：mem_xxx |
| `agent_id` | string | 是 | 外键，关联agents.id，记忆所属Agent |
| `conversation_id` | string | 否 | 外键，关联conversations.id，标记会话级来源 |
| `source_task_id` | string | 否 | 外键，关联tasks.id，标记来源任务 |
| `source_job_id` | string | 否 | 外键，关联memory_consolidation_jobs.id，标记来源整理作业 |
| `source_type` | enum | 是 | 来源类型：manual（手动创建）、task_output（任务输出）、attachment_digest（附件摘要）、daily_consolidation（定时整理） |
| `scope` | enum | 是 | 记忆范围：conversation（会话级）、history（历史级）、manual（手动级） |
| `summary_date` | date | 否 | 若为定时整理生成，记录摘要对应日期 |
| `source_conversation_ids` | json | 否 | 本条记忆关联的会话ID数组 |
| `dedup_key` | string | 否 | 同一Agent、同一日期范围的定时整理幂等去重键 |
| `summary_version` | int | 否 | 定时整理摘要版本号，用于补跑后的来源追踪 |
| `title` | string | 是 | 记忆标题 |
| `content` | text | 是 | 记忆正文内容 |
| `summary` | text | 否 | 记忆摘要，用于快速预览 |
| `status` | enum | 是 | 状态：active（活跃）、archived（归档）、deleted（已删除） |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |
| `last_used_at` | datetime | 否 | 最后使用时间 |

### 6.9 task_memory_links（任务记忆关联表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：tml_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `memory_entry_id` | string | 是 | 外键，关联memory_entries.id |
| `link_type` | enum | 是 | 关联类型：loaded（加载）、written（写入）、updated（更新） |
| `reason` | text | 否 | 关联原因或摘要 |
| `created_at` | datetime | 是 | 创建时间 |

### 6.10 task_write_commands（任务写命令表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：twc_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `command_type` | enum | 是 | 写命令类型：step_completed（步骤完成）、permission_resolved（权限已决）、retry_requested（重试请求）、degrade_applied（已降级） |
| `expected_aggregate_version` | int | 是 | 提交写命令时声明的任务聚合版本 |
| `status` | enum | 是 | 写命令状态：queued（排队中）、processing（处理中）、applied（已应用）、rejected（已拒绝） |
| `conflict_code` | string | 否 | 冲突错误码，如TASK_WRITE_CONFLICT |
| `payload` | json | 是 | 写命令原始载荷 |
| `created_at` | datetime | 是 | 创建时间 |
| `processed_at` | datetime | 否 | 处理完成时间 |

### 6.11 permission_requests（权限请求表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：perm_req_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `step_id` | string | 否 | 外键，关联task_steps.id |
| `agent_id` | string | 否 | 外键，关联agents.id，发起权限请求的Agent |
| `skill_id` | string | 否 | 关联的Skill ID |
| `action_type` | enum | 是 | 动作类型：read（读）、write（写）、edit（编辑）、exec（执行）、external_call（外部调用） |
| `target` | string | 是 | 目标资源或目标系统标识 |
| `permission_mode` | enum | 是 | 权限策略模式：allow（允许）、ask（需确认）、deny（拒绝） |
| `status` | enum | 是 | 请求状态：pending（待确认）、approved（已批准）、rejected（已拒绝）、expired（已过期） |
| `reason` | text | 否 | 触发权限请求的原因说明 |
| `decision_source` | enum | 是 | 决策来源：system_policy（系统策略）、user_confirmation（用户确认） |
| `decision_trace_summary` | text | 否 | 权限判定轨迹摘要 |
| `decision_trace_payload` | json | 否 | 权限判定详细轨迹 |
| `confirmation_version` | int | 是 | 权限确认版本号，用于并发乐观锁控制 |
| `expected_sequence` | int | 是 | 创建请求时的事件序号快照 |
| `resolved_sequence` | int | 否 | 决策形成时的事件序号 |
| `requested_at` | datetime | 是 | 请求创建时间 |
| `resolved_at` | datetime | 否 | 决策完成时间 |

### 6.12 task_trigger_subscriptions（任务触发订阅表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：tts_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `trigger_mode` | enum | 是 | 入口等待类型：queued（排队等待）、scheduled（定时等待）、event_triggered（事件触发） |
| `status` | enum | 是 | 订阅状态：active（活跃）、triggered（已触发）、invalidated（已失效）、cancelled（已取消）、expired（已过期） |
| `subscription_key` | string | 是 | 触发订阅唯一标识 |
| `condition_snapshot` | json | 是 | 当前等待条件快照 |
| `threshold_config_snapshot` | json | 否 | 阈值配置快照 |
| `queue_release_policy` | string | 是 | 队列释放策略，P0固定为fifo |
| `slot_budget_scope` | string | 是 | 槽位预算范围，P0固定为global_runtime_slots |
| `domain_agent_id` | string | 否 | 所属Domain Agent ID |
- `release_token`：一次性释放认领标识；首次成功释放后写入，后续重复释放请求不得覆盖。
- `last_trigger_fact_id`：可选，最近一次命中的触发事实唯一标识。
- `last_trigger_source_type`：可选，最近一次命中的触发事实来源类型，如 `queue_slot`、`timer_due`、`domain_event`。
- `last_trigger_source_ref`：可选，最近一次命中的触发事实来源引用。
- `last_trigger_source_signature`：可选，最近一次命中的触发事实签名或摘要，用于可信校验和重复投递抑制。
- `last_evaluated_at`：最近一次重评估时间。
- `next_check_at`：可选，下一次预计重评估时间。
- `triggered_at`：可选，条件满足并释放任务的时间。
- `invalidated_at`：可选，订阅失效时间。
- `invalidation_reason`：可选，失效原因，如 `task_cancelled`、`task_timeout`、`task_failed`、`manual_closed`、`trigger_condition_invalid`、`snapshot_invalidated`。
- `created_at` / `updated_at`。

### 6.13 permission_decision_logs（权限决策日志表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：pdl_xxx |
| `permission_request_id` | string | 是 | 外键，关联permission_requests.id |
| `decision` | enum | 是 | 权限决策结果：approved（批准）、rejected（拒绝）、auto_allowed（自动允许）、auto_denied（自动拒绝） |
| `decided_by` | string | 是 | 决策人ID，system表示系统自动决策 |
| `decision_source` | enum | 是 | 决策来源：system_default_allow（系统默认允许）、system_default_deny（系统默认拒绝）、user_confirmation（用户确认） |
| `confirmation_version` | int | 是 | 本次决策命中的权限确认版本号 |
| `expected_sequence` | int | 是 | 提交确认时的事件序号 |
| `event_sequence` | int | 是 | 本次决策对应的事件序号 |
| `decision_trace_summary` | text | 否 | 权限判定轨迹摘要 |
| `normalized_error_code` | string | 否 | 若因冲突失败，记录归一化错误码 |
| `comment` | text | 否 | 备注信息 |
| `created_at` | datetime | 是 | 创建时间 |

### 6.14 task_events（任务事件表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：evt_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `step_id` | string | 否 | 外键，关联task_steps.id |
| `agent_id` | string | 否 | 外键，关联agents.id |
| `sequence` | int | 是 | 任务内严格递增事件序号 |
| `cursor` | string | 是 | 事件游标字符串，用于恢复和分页 |
| `event_type` | string | 是 | 事件类型 |
| `status` | enum | 是 | 事件状态：pending（待处理）、processed（已处理）、failed（失败） |
| `summary` | text | 是 | 时间线展示摘要 |
| `payload` | json | 是 | 事件业务载荷 |
| `created_at` | datetime | 是 | 创建时间 |

### 6.15 model_call_logs（模型调用日志表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：mcl_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `step_id` | string | 否 | 外键，关联task_steps.id |
| `provider` | string | 是 | 模型提供方标识，如openai、anthropic、local |
| `model_name` | string | 是 | 模型名称，如gpt-4、claude-3 |
| `request_fingerprint` | string | 是 | 请求指纹，用于幂等标识 |
| `status` | enum | 是 | 调用状态：started（已开始）、succeeded（成功）、failed（失败）、fallback_succeeded（回退成功） |
| `normalized_error_code` | string | 否 | 归一化后的失败码 |
| `retry_attempt` | int | 是 | 当前重试次数 |
| `fallback_provider` | string | 否 | 回退目标提供方 |
| `latency_ms` | int | 否 | 调用耗时（毫秒） |
| `token_usage` | json | 否 | token使用量，包含prompt_tokens、completion_tokens、total_tokens |
| `error_message` | text | 否 | 错误信息 |
| `created_at` | datetime | 是 | 调用发起时间 |
| `completed_at` | datetime | 否 | 调用完成时间 |

### 6.16 memory_consolidation_jobs（记忆整理作业表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：mcj_xxx |
| `agent_id` | string | 是 | 外键，关联agents.id |
| `summary_date` | date | 是 | 整理对应的日期 |
| `trigger_mode` | enum | 是 | 触发方式：scheduled（定时）、manual_rerun（手动补跑） |
| `triggered_by` | string | 否 | 手动补跑的操作者ID |
| `status` | enum | 是 | 作业状态：queued（排队中）、running（执行中）、succeeded（成功）、failed（失败）、deduplicated（已去重） |
| `source_conversation_ids` | json | 是 | 参与整理的会话ID数组 |
| `generated_memory_entry_ids` | json | 否 | 本次生成的记忆条目ID数组 |
| `dedup_key` | string | 否 | 去重键 |
| `summary_version` | int | 否 | 生成的摘要版本号 |
| `error_message` | text | 否 | 失败原因 |
| `started_at` | datetime | 否 | 开始时间 |
| `completed_at` | datetime | 否 | 完成时间 |
| `created_at` | datetime | 是 | 创建时间 |

### 6.17 tool_invocation_logs（工具调用日志表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：til_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `step_id` | string | 否 | 外键，关联task_steps.id |
| `skill_id` | string | 否 | 外键，关联skills.id |
| `tool_name` | string | 是 | 工具名称 |
| `tool_version` | string | 否 | 工具版本 |
| `invocation_type` | enum | 是 | 调用类型：direct（直接调用）、skill_proxy（Skill代理） |
| `status` | enum | 是 | 调用状态：invoked（已调用）、succeeded（成功）、failed（失败）、timeout（超时） |
| `input_summary` | text | 是 | 输入摘要 |
| `output_summary` | text | 否 | 输出摘要 |
| `error_message` | text | 否 | 错误信息 |
| `latency_ms` | int | 否 | 调用耗时 |
| `permission_mode` | enum | 是 | 权限模式：allow（允许）、ask（需确认）、deny（拒绝） |
| `permission_decision` | enum | 是 | 权限决策结果 |
| `created_at` | datetime | 是 | 调用发起时间 |
| `completed_at` | datetime | 否 | 调用完成时间 |

### 6.18 knowledge_hit_logs（知识命中日志表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：khl_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `step_id` | string | 否 | 外键，关联task_steps.id |
| `query` | text | 是 | 检索查询语句 |
| `retrieved_documents` | json | 是 | 检索到的文档列表 |
| `hit_count` | int | 是 | 命中数量 |
| `relevance_scores` | json | 否 | 各文档相关性分数数组 |
| `selected_documents` | json | 否 | 实际选中的文档列表 |
| `status` | enum | 是 | 检索状态：completed（完成）、failed（失败） |
| `latency_ms` | int | 否 | 检索耗时 |
| `created_at` | datetime | 是 | 创建时间 |

### 6.19 agents（Agent表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：agent_xxx |
| `name` | string | 是 | Agent名称 |
| `type` | enum | 是 | Agent类型：leader（领导者）、domain（领域） |
| `description` | text | 否 | Agent描述 |
| `model_provider` | string | 是 | 模型提供方，如openai、anthropic |
| `model_name` | string | 是 | 模型名称 |
| `capabilities` | json | 否 | Agent能力列表 |
| `status` | enum | 是 | 状态：active（活跃）、inactive（未激活）、deleted（已删除） |
| `is_system` | boolean | 是 | 是否为系统内置Agent |
| `config` | json | 否 | Agent配置JSON |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

### 6.20 skills（Skill表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：skill_xxx |
| `name` | string | 是 | Skill名称 |
| `description` | text | 否 | Skill描述 |
| `instructions` | text | 是 | 指令集，说明Agent如何使用此Skill |
| `version` | string | 是 | 版本号，格式：x.y.z |
| `runtime_language` | enum | 是 | 运行时语言：node、python、ruby、bash、powershell |
| `status` | enum | 是 | 状态：draft（草稿）、active（活跃）、inactive（未激活）、deleted（已删除） |
| `permissions` | json | 否 | 权限配置，包含read/write/execute的allow/ask/deny策略 |
| `tools` | json | 否 | 工具定义列表 |
| `metadata` | json | 否 | 扩展元数据 |
| `activated_at` | datetime | 否 | 激活时间 |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

### 6.21 permission_policies（权限策略表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：policy_xxx |
| `name` | string | 是 | 策略名称 |
| `scope` | enum | 是 | 作用域：global（全局）、agent、skill、tool、resource |
| `target_id` | string | 否 | 目标ID，如Agent ID、Skill ID |
| `action_type` | enum | 是 | 动作类型：read、write、exec、external_call |
| `decision` | enum | 是 | 权限决策：allow（允许）、ask（需确认）、deny（拒绝） |
| `priority` | int | 是 | 优先级，数值越大优先级越高 |
| `conditions` | json | 否 | 条件配置 |
| `status` | enum | 是 | 状态：active、inactive、deleted |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

### 6.22 system_configs（系统配置表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：config_xxx |
| `key` | string | 是 | 配置键，唯一标识 |
| `value` | json | 是 | 配置值 |
| `type` | enum | 是 | 配置类型：string、number、boolean、json |
| `category` | enum | 是 | 分类：system、model、feature |
| `description` | text | 否 | 配置描述 |
| `is_sensitive` | boolean | 是 | 是否敏感配置 |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

### 6.23 prompt_templates（Prompt模板表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：pt_xxx |
| `name` | string | 是 | 模板名称 |
| `type` | enum | 是 | 模板类型：leader、domain（禁止 output） |
| `description` | text | 否 | 模板描述 |
| `current_version` | int | 是 | 当前版本号 |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

### 6.24 prompt_versions（Prompt版本表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：pv_xxx |
| `template_id` | string | 是 | 外键，关联prompt_templates.id |
| `version` | int | 是 | 版本号 |
| `system` | text | 否 | system 段文本 |
| `developer` | text | 否 | developer 段文本 |
| `user` | text | 否 | user 段文本 |
| `context` | text | 否 | context 段文本 |
| `tool_result` | text | 否 | tool_result 段文本 |
| `slots` | json(text) | 否 | 插槽定义 |
| `change_log` | text | 否 | 变更原因 |
| `created_at` | datetime | 是 | 创建时间 |

### 6.24A agent_prompt_profiles（Agent Prompt 配置表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：app_xxx |
| `agent_id` | string | 是 | 外键，关联agents.id |
| `template_id` | string | 否 | 外键，关联prompt_templates.id |
| `template_version` | int | 否 | 当前绑定模板版本 |
| `role_definition` | text | 否 | Agent 角色定位内容 |
| `behavior_norm` | text | 否 | Agent 行为规范 |
| `capability_boundary` | text | 否 | Agent 能力边界说明 |
| `custom_slots` | json(text) | 否 | Agent 自定义插槽值 |
| `version` | int | 是 | Agent Prompt 配置版本 |
| `created_at` | datetime | 是 | 创建时间 |
| `updated_at` | datetime | 是 | 更新时间 |

### 6.24B prompt_migration_records（Prompt 迁移记录表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：pmr_xxx |
| `agent_id` | string | 是 | 外键，关联agents.id |
| `from_template_id` | string | 否 | 外键，关联prompt_templates.id |
| `from_version` | int | 否 | 迁移前模板版本 |
| `to_template_id` | string | 否 | 外键，关联prompt_templates.id |
| `to_version` | int | 否 | 迁移目标模板版本 |
| `status` | enum | 是 | 迁移状态：success、failed、rollback |
| `compatibility_result` | enum | 否 | 兼容性结果：compatible、incompatible、warning |
| `slot_mappings` | json(text) | 否 | 插槽映射详情 |
| `conflicts` | json(text) | 否 | 冲突详情 |
| `error_message` | text | 否 | 错误信息 |
| `migrated_at` | datetime | 否 | 迁移执行时间 |
| `created_at` | datetime | 是 | 创建时间 |

### 6.25 task_checkpoints（任务检查点表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：cp_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `step_id` | string | 否 | 外键，关联task_steps.id |
| `checkpoint_data` | json | 是 | 检查点数据，包含任务状态、工具调用、输入输出摘要 |
| `validation_result` | json | 是 | 验证结果，包含integrity、idempotency、timeliness |
| `status` | enum | 是 | 状态：valid（有效）、invalid（无效）、expired（已过期） |
| `created_at` | datetime | 是 | 创建时间 |

### 6.26 trigger_evaluations（触发评估表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：te_xxx |
| `task_id` | string | 是 | 外键，关联tasks.id |
| `trigger_type` | enum | 是 | 触发类型：queued、scheduled、event_triggered |
| `can_release` | boolean | 是 | 是否可以释放 |
| `evaluation_summary` | text | 是 | 评估摘要 |
| `next_check_at` | datetime | 否 | 下次检查时间 |
| `anomaly_detected` | boolean | 是 | 是否检测到异常 |
| `anomaly_code` | string | 否 | 异常码 |
| `anomaly_summary` | string | 否 | 异常摘要 |
| `created_at` | datetime | 是 | 创建时间 |

### 6.27 audit_logs（审计日志表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：audit_xxx |
| `user_id` | string | 是 | 操作人ID，system表示系统操作 |
| `action_type` | string | 是 | 操作类型 |
| `resource_type` | string | 是 | 资源类型 |
| `resource_id` | string | 是 | 资源ID |
| `result` | enum | 是 | 结果：success、failed、rejected |
| `ip_address` | string | 否 | 客户端IP |
| `user_agent` | string | 否 | 客户端User-Agent |
| `request_id` | string | 否 | 请求ID |
| `details` | json | 否 | 详情JSON |
| `created_at` | datetime | 是 | 创建时间 |

### 6.28 attachment_events（附件事件表）

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | 是 | 主键，格式：ae_xxx |
| `attachment_id` | string | 是 | 外键，关联attachments.id |
| `event_type` | enum | 是 | 事件类型：uploaded、parsed、reparsed、deleted |
| `status` | enum | 是 | 状态 |
| `details` | json | 否 | 详情JSON |
| `created_at` | datetime | 是 | 创建时间 |

## 7. 主键、外键与索引

### 7.1 主键规则
- 主键统一使用字符串 ID
- 表间引用统一使用同长度字符串

### 7.2 外键规则
- messages.conversation_id → conversations.id
- attachments.conversation_id → conversations.id
- attachments.task_id → tasks.id
- tasks.conversation_id → conversations.id
- task_steps.task_id → tasks.id
- task_steps.attachment_id → attachments.id
- task_outputs.task_id → tasks.id
- knowledge_base_documents.source_attachment_id → attachments.id
- memory_entries.conversation_id → conversations.id
- memory_entries.source_task_id → tasks.id
- memory_entries.source_job_id → memory_consolidation_jobs.id
- task_memory_links.task_id → tasks.id
- task_memory_links.memory_entry_id → memory_entries.id
- task_write_commands.task_id → tasks.id
- permission_requests.task_id → tasks.id
- permission_requests.step_id → task_steps.id
- permission_decision_logs.permission_request_id → permission_requests.id
- task_trigger_subscriptions.task_id → tasks.id
- task_events.task_id → tasks.id
- task_events.step_id → task_steps.id
- model_call_logs.task_id → tasks.id
- model_call_logs.step_id → task_steps.id
- memory_consolidation_jobs.agent_id → agents.id
- tool_invocation_logs.task_id → tasks.id
- tool_invocation_logs.step_id → task_steps.id
- tool_invocation_logs.skill_id → skills.id
- knowledge_hit_logs.task_id → tasks.id
- knowledge_hit_logs.step_id → task_steps.id
- prompt_versions.prompt_template_id → prompt_templates.id
- agent_prompt_profiles.agent_id → agents.id
- agent_prompt_profiles.prompt_template_id → prompt_templates.id
- prompt_migration_records.agent_id → agents.id
- prompt_migration_records.prompt_template_id → prompt_templates.id
- task_checkpoints.task_id → tasks.id
- task_checkpoints.step_id → task_steps.id
- trigger_evaluations.task_id → tasks.id

### 7.3 索引规则
- 所有状态字段建立索引
- 所有时间字段建立索引
- conversation_id + created_at 联合索引
- task_id + status 联合索引
- agent_id + created_at 联合索引
- task_id + sequence 唯一联合索引
- task_id + cursor 唯一联合索引
- task_id + aggregate_version 联合索引

## 8. 状态枚举

### 8.1 Task.status

- `pending`
- `clarification_pending`
- `planning`
- `running`
- `completed`
- `partial_failed`
- `failed`
- `cancelled`
- `timeout`
- `intervention_required`

### 8.2 TaskStep.type

- `intent_analysis`
- `attachment_parse`
- `knowledge_retrieval`
- `agent_execution`
- `tool_invocation`
- `result_synthesis`

### 8.3 Attachment.parse_status

- `queued`
- `parsing`
- `parsed`
- `failed`
- `accepted`
- `skipped`

## 9. 数据约束

- `tasks` 只维护当前任务状态，不保存完整步骤明细。
- `tasks.aggregate_version` 是任务聚合写入的乐观锁基线；所有进入 `task_write_commands` 的写命令都必须声明预期版本或在等价队列顺序下被消费。
- `task_events` 是时间线和实时推送的事实源。
- `task_outputs` 是结果交付唯一来源，不从消息表反推最终结果。
- 知识库资料生命周期独立于会话和任务。
- `memory_entries` 为 Agent 级持久记忆主表，是否在某次任务中被加载或写入，由 `task_memory_links` 记录。
- `memory_entries.source_type = daily_consolidation` 的记录表示后台定时整理生成的会话日摘要，必须携带 `summary_date`、`source_conversation_ids` 和稳定 `dedup_key`，避免重复运行时生成语义等价的多份长期记忆。
- `memory_entries.source_job_id` 用于把一条持久记忆回溯到具体整理作业；查询日摘要来源时，应优先返回 `source_job_id`、`summary_version` 与 `source_conversation_ids` 组合，而不是依赖非结构化摘要文本反推来源。
- `task_write_commands` 用于记录任务级串行化写命令及其冲突结果，任何因过期聚合版本被拒绝的写入都不得直接覆盖 `tasks` 当前状态。
- `task_trigger_subscriptions` 用于记录排队、定时和事件触发任务的等待条件与释放情况；等待态任务不得只依赖内存定时器或临时订阅状态维持可恢复性。
- `tasks.intake_input_summary`、`tasks.memory_scope` 与 `tasks.selected_memory_entry_ids_snapshot` 共同构成延迟任务的入口上下文冻结快照；任务进入等待态后，释放执行时必须默认基于该快照继续执行，不得隐式改写为释放时刻的最新会话上下文。
- 当 `tasks.status = clarification_pending` 时，必须同时通过 `tasks.clarification_required_fields`、`tasks.trigger_decision_summary` 或等价字段表达待澄清原因；后续补充信息、默认确认或关闭动作都必须更新原 `tasks.id` 对应记录，不得通过重建第二条任务记录替代原单。
- `tasks.complexity_decision_summary` 用于沉淀复杂任务判定事实；若系统创建了 Leader 父任务和 Domain 子任务，则该字段不得为空或仅保留不可解释的模型原始文本。
- 若冻结快照中的显式记忆条目在释放前失效，必须把失效条目写入 `tasks.invalid_memory_entry_ids`，并以 `tasks.snapshot_resolution` 明确记录处理结果；系统不得静默切换到释放时刻的新记忆集合重算入口决策。
- 当 `tasks.snapshot_resolution = manual_intervention_required` 时，任务必须进入 `TaskInterventionRequired` 终态并停止继续释放，不得被映射为 `TaskManuallyClosed` 或普通失败。
- 当任务因等待时间过长、触发条件失效或快照失效而不能继续自动推进时，必须写入 `tasks.waiting_anomaly_code`、`tasks.waiting_anomaly_summary`、`tasks.last_trigger_evaluation_summary`、`tasks.waiting_threshold_mode`、`tasks.waiting_threshold_basis_summary`，并在需要人工介入时同时写入 `tasks.intervention_required_reason`；系统不得仅依赖临时内存日志表达等待异常。
- `task_trigger_subscriptions.release_token` 用于保证同一等待订阅只能成功释放一次；若重复命中相同条件，只能视为幂等重放或冲突，不得再次推进任务主状态。
- `queued` 任务在 P0 阶段按 FIFO 释放，因此 `queue_position` 必须与订阅建立顺序一致；取消队列任务后允许后续任务顺位前移，但不得改变未取消任务之间的相对顺序。其释放判断只允许基于 `slot_budget_scope = global_runtime_slots` 的全局预算，不得额外引入未声明的子队列。
- 对分配给 Domain Agent 的 `queued` 子任务，`queue_position` 必须解释为该任务在所属 Agent 显式队列中的顺位；父任务的 ETA 由各子任务所在 Agent 队列顺位、已知触发时间和历史平均执行时长共同估算。
- 父任务与子任务必须通过 `tasks.parent_task_id` 建立关系；父任务只有在全部必需子任务都达到 `queued_confirmed`、`trigger_registered` 或等价安排完成状态后，才能写入 `arrangement_status=arranged_completed` 并发出 `TaskArrangementCompleted`。
- 父任务与子任务必须通过 `tasks.parent_task_id` 建立关系；父任务只有在全部必需子任务都达到 `queued_confirmed`、`trigger_registered` 或等价安排完成状态后，才能写入 `arrangement_status=arranged_completed` 并发出 `TaskArrangementCompleted`。`arrangement_status=arranged_completed` 只表达安排链路完成，不得被解释为权限确认已全部完成、最终输出已生成或质量增强检查已通过。
- `task_outputs.output_stage = arranged_only` 的记录只用于表达阶段性安排通知或 ETA 摘要，不得被标记为最终主输出；只有 `output_stage = final` 的输出才允许作为结果页最终交付物和知识沉淀来源。
- 对于跨端一次生效型交互，数据库必须保证 `tasks.interaction_dedup_key` 或等价事件载荷在同一任务事实范围内只产生一个最终有效处理结果；后续重复交互只能追加审计事实，不得再次改写主事实字段。
- 若父任务以部分完成或降级交付方式结束，则必须把可审计的结构化判定写入 `task_outputs.degraded_delivery_summary`，不得只在 `tasks.degrade_reason` 或非结构化摘要中保留“主要目标已达成”的结论。
- 若父任务在 `task_outputs.output_stage = final` 后收到晚到 non-blocking 成功结果，则只能把补充事实追加到 `task_outputs.supplemental_updates` 和 / 或 `task_events.payload.supplementalUpdate`，不得覆盖原 `task_outputs.content`、不得回写成第二条首版最终输出，也不得改变首次最终交付对应的时间线事实。
- 若 `task_outputs.supplemental_updates` 非空且本次补充结果对用户产生再次通知，则必须同时写入 `task_outputs.supplemental_notification_type`；系统不得只通过非结构化消息文本推断补充更新通知语义。
- 若任务合法结束但没有主输出物，则必须写入 `task_outputs.terminal_summary`；结果页或终态摘要页不得依赖临时拼装文案替代持久化终态摘要。
- 若 blocking / non-blocking 标记在已参与安排、ETA 计算或交付判定后发生变化，必须通过 `task_events` 追加 `BlockingDecisionChanged` 事件，并在载荷中记录 `blockingDecisionChangeSummary`；系统不得静默覆盖子任务标记后仅保留最终状态。
- `task_trigger_subscriptions` 在任务取消、超时、失败终止或人工关闭后必须转为 `invalidated`、`cancelled` 或 `expired`，不得继续参与后续触发评估。
- `TaskTriggerInvalidated` 不得单独落账；当订阅失效时，必须与任务终态变更同提交，并在 `invalidation_reason` 中明确标记 `task_cancelled`、`task_timeout`、`task_failed` 或 `manual_closed` 等原因。
- 成功释放的延迟任务必须按 `TaskTriggerActivated -> TaskReadyForPlanning -> MemoryScopeConfirmed` 顺序落账；`TaskTriggerReevaluated` 只用于未释放的重评估，不得替代成功释放事件。
- `Task Intake Service` 是等待态协调的唯一写入驱动者；数据库层不得依赖内存态队列扫描器或临时定时器句柄作为唯一恢复依据，所有等待态恢复所需事实必须可由 `tasks` 与 `task_trigger_subscriptions` 重建。
- `memory_consolidation_jobs` 用于记录定时整理和手动补跑作业；同一 Agent、同一日期范围在已有运行中作业时，不得再接受第二个并发补跑请求，接口层统一返回 `MEMORY_CONSOLIDATION_ALREADY_RUNNING`。
- `permission_requests` 用于记录待权限确认和已完成权限决策的高风险动作，最终权限决策轨迹由 `permission_decision_logs` 保存，并通过 `confirmation_version` 控制并发确认覆盖。
- `model_call_logs` 用于沉淀模型调用事实和失败归一化结果，业务模块不直接依赖供应商原始错误码进行持久化。
- `task_events.sequence` 是任务内唯一稳定排序依据，`cursor` 是对外恢复协议字段；两者必须与 `tasks.last_event_sequence`、`tasks.last_event_cursor` 保持一致更新。
- 因过期 `expected_sequence` 或过期 `confirmation_version` 触发的权限确认冲突，不得写入新的权限决策日志或权限通过/拒绝事件，接口层统一返回 `PERMISSION_CONFIRMATION_CONFLICT`。
- 因过期 `aggregate_version` 或任务写队列冲突触发的任务写入失败，不得直接覆盖任务聚合当前状态，接口层统一返回 `TASK_WRITE_CONFLICT`。

`task_events` 事件载荷统一采用以下结构：

事件表及其 `payload` 统一承接任务时间线、入口等待与澄清、父子任务安排、双阶段交付、补充更新通知、多端显示范围和写入冲突等可审计事实；来源：N-01.2、N-01.4、N-01.5、N-02.2、N-02.4、N-02.5、N-03.1、N-03.2、N-03.3、N-04.2、N-04.3、N-04.4、N-07.2、N-07.3、N-07.5、N-09.2、N-09.4、N-09.5、N-13、N-14。

- `id`：事件 ID。
- `task_id`：所属任务。
- `sequence`：单任务内严格递增序号。
- `cursor`：事件游标。
- `event_type`：事件类型，如 `TaskCreated`、`TaskTriggerResolved`、`TaskTriggerWaiting`、`TaskReadyForPlanning`、`TaskPlanned`、`SubTasksCreated`、`DomainTaskQueued`、`DomainTaskScheduled`、`DomainTaskTriggered`、`TaskArrangementCompleted`、`TaskEtaEstimated`、`GoalSatisfactionEvaluated`、`TaskReplanned`、`TaskWriteQueued`、`TaskWriteConflictDetected`、`BlockingDecisionChanged`、`StepStarted`、`TaskCompleted`、`TaskPartialFailed`、`TaskFailed`、`TaskCancelled`、`TaskTimedOut`、`TaskManuallyClosed`、`TaskInterventionRequired`、`RouteResolved`、`PermissionCheckRequested`、`PermissionGranted`、`PermissionDenied`、`StepParallelQueued`、`StepWaiting`、`TaskTriggerReevaluated`、`TaskTriggerActivated`、`TaskTriggerInvalidated`、`TaskAutoRetried`、`TaskDegraded`、`MemoryScopeConfirmed`、`MemoryLoaded`、`MemoryRead`、`MemoryWritten`、`ModelCallStarted`、`ModelCallCompleted`、`ModelCallFailed`。
- `step_id`：可选，关联步骤。
- `agent_id`：可选，关联 Agent。
- `status`：事件状态。
- `summary`：供时间线直接展示的摘要文本。
- `payload`：JSON 对象，按事件类型承载 `decisionReason`、`selectedAgents`、`parallelGroupId`、`waitingReason`、`memoryScope`、`memoryEntryIds`、`triggerMode`、`triggerStatus`、`triggerDecisionSummary`、`complexityDecisionSummary`、`nextTriggerCheckAt`、`triggerRule`、`clarificationRequiredFields`、`clarificationResolutionSummary`、`clarificationClosedReason`、`subscriptionId`、`releaseToken`、`triggerFactId`、`triggerSourceType`、`triggerSourceRef`、`triggerSourceSignature`、`invalidationReason`、`lastTriggerEvaluationSummary`、`waitingAnomalyCode`、`waitingAnomalySummary`、`waitingThresholdMode`、`waitingThresholdBasisSummary`、`interventionRequiredReason`、`suggestedActions`、`permissionMode`、`permissionRequestId`、`decisionSource`、`decisionTraceSummary`、`aggregateVersion`、`expectedAggregateVersion`、`writeCommandId`、`conflictCode`、`modelProvider`、`normalizedErrorCode`、`retryAttempt`、`fallbackProvider`、`target`、`retrySource`、`degradeReason`、`degradeAction`、`degradedDeliverySummary`、`supplementalUpdate`、`supplementalNotificationType`、`blockingDecisionChangeSummary`、`terminalSummary`、`reasoningSummary`、`nextActionSummary`、`observationSummary`、`observationSourceType`、`observationRef`、`usedObservationIds`、`planRevision`、`replanCount`、`latestPlanSummary`、`goalSatisfactionStatus`、`goalSatisfactionSummary`、`replanReason` 等字段，用于表达入口等待、正式待澄清状态、复杂任务判定、一次性释放、触发事实身份、命中的阈值依据、等待异常、订阅失效、权限策略、权限请求、写队列冲突、降级交付判定、补充更新通知、blocking 改标事实、模型失败归一化，以及 ReAct 回合中的简短推理、观察摘要、目标满足性判断与增量重编排事实等上下文信息。
- `payload`：JSON 对象，在单窗口主交互和多端显示场景下还需承载 `entryPoint`、`originClientId`、`syncPolicy`、`visibleClientIds`、`displayScope` 等字段，用于表达该事件默认在哪些接入端显示，以及是否仅在发起端展示输入、进度、结果和权限授权信息。
- `payload`：JSON 对象，在一次生效型交互场景下还需承载 `interactionDedupKey`、`interactionDecisionStatus`、`resolvedByClientId`、`resolvedAt`、`factSummary` 等字段，用于表达同一事实是否已被其他接入端处理、最终处理结果以及跨端渲染最小事实摘要所需内容。
- `payload`：JSON 对象，在冻结快照校验或延迟任务释放场景下还需承载 `intakeInputSummary`、`selectedMemoryIdsSnapshot`、`invalidMemoryEntryIds`、`snapshotResolution`、`minimumContextSatisfied` 等字段，用于表达入口快照、失效条目与降级处理结果。
- `payload`：JSON 对象，在父子任务和安排确认场景下还需承载 `parentTaskId`、`childTaskId`、`assignedDomainAgentId`、`arrangementStatus`、`arrangementSummary`、`queuePositionInAgent`、`estimatedCompletionAt`、`estimatedDurationMinutes`、`userNotificationStage`、`finalOutputReady`、`outputStage`、`arrangementNoticeSummary` 等字段，用于表达 Domain 子任务安排确认、父任务 ETA 聚合、双阶段用户通知以及阶段性通知与最终交付的区别。
- `payload`：JSON 对象，在 `GoalSatisfactionEvaluated`、`TaskReplanned` 或等价父任务增量编排事件中还需承载 `planRevision`、`replanCount`、`goalSatisfactionStatus`、`goalSatisfactionSummary`、`replanReason`、`usedObservationIds`、`latestPlanSummary`，用于保证“基于哪些观察得出当前目标未满足并触发下一轮编排”可被稳定追溯；若 `goalSatisfactionStatus=satisfied`，则不得再生成新的 `TaskReplanned` 事件。
- `created_at`：事件创建时间。

## 10. 数据库文档结论

数据库设计必须服务于三个目标：

- 支撑任务执行。
- 支撑过程追踪。
- 支撑结果复盘与长期沉淀。

## 11. 版本增补（2026-03-19）

- `knowledge_documents.source_type` 新增值：`file_upload`（文件上传入库）。
- `knowledge_documents.vector_ids` 使用 JSON 字符串保存：`{ chunkIds: string[], overview: string }`。
- `overview` 作为知识文档总览字段由 LLM 生成并通过 `vector_ids` 元数据回填。
- LanceDB 分片表通过 `docId` 与 SQLite 主表关联，删除文档需同步清理 LanceDB 分片。