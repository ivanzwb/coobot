# BiosBot 概要设计

## 1. 文档目的

本文档用于定义 BiosBot 的系统组成、分层结构、模块边界、运行机制和核心链路。
本文档主要支撑 01-需求分析 中的主链路需求 N-01～N-05，以及服务和模块层面的支撑需求 N-06～N-14。
本文档重点回答"模块如何拆分、职责如何归属、链路如何衔接"；具体技术机制、实现策略和风控细节以技术分析文档为准。

## 2. 系统目标

- 建立本地优先的多智能体任务系统。
- 以任务为中心统一会话、附件、知识、工具与结果。
- 让前端、后端、模型、知识库和工具形成单一主链路。

## 2.1 产品范围与用户角色

| 定位 | 说明 |
|------|------|
| 平台类型 | 本地优先的多智能体协作平台 |
| 输入类型 | 文本、图片、文档等 |
| 核心闭环 | Leader Agent + Domain Agent + Skills + 知识库 + 任务追踪 |
| 用户规模 | P0 默认单用户/小团队，不构建多租户体系 |

**目标用户：**

| 用户类型 | 关注点 |
|----------|--------|
| 个人开发者与 AI 爱好者 | 本地部署、Agent 配置、Skills、工具与实验能力 |
| 专业知识工作者 | 资料分析、结构化输出、引用依据和过程可复核 |
| 小团队负责人 | 任务复盘、失败定位、知识沉淀和流程复用 |

**P0 核心目标：**

用户能够**上传文本和附件**，发起复杂任务，看到系统如何**拆解与执行**，并获得**可复用、可解释**的最终结果。

## 3. 系统分层架构

### 3.1 分层总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        接入层 (Layer 1)                          │
│   Web前端  │  HTTP API  │  WebSocket  │  (未来扩展: CLI/Mobile) │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                        应用层 (Layer 2)                          │
│  Conversation Service  │  Task Intake Service  │  Task Service │
│  Attachment Service     │  Knowledge Base Service              │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                        编排层 (Layer 3)                          │
│  Task Orchestrator  │  Intent Resolver  │  Plan Builder        │
│  Capability Matcher │  Execution Scheduler │  Run Finalizer     │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                      领域能力层 (Layer 4)                        │
│  Input Normalization  │  Trigger Evaluation  │  Agent Execution│
│  Knowledge Retrieval  │  Skill Invocation    │  Output Service │
│  Tracking & Event     │  Knowledge Ingestion │ Permission Policy│
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                      基础设施层 (Layer 5)                        │
│  SQLite  │  LanceDB  │  文件系统  │  LLM Adapter  │  日志/审计  │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 接入层

| 模块 | 职责 | 关联需求 |
|------|------|----------|
| Web 前端 | 界面渲染、用户交互、实时展示 | N-01, N-04 |
| HTTP API | 请求接收、参数校验、响应封装 | N-01, N-02 |
| WebSocket | 实时事件推送、断线恢复 | N-01, N-04, N-14 |

### 3.3 应用层

| 服务 | 职责 | 关联需求 |
|------|------|----------|
| Conversation Service | 会话管理、消息存储、最近任务引用 | N-01, N-05 |
| Task Intake Service | 入口判定、等待订阅、触发释放、**入口澄清** | N-01, N-01.5, N-02 |
| Task Service | 任务聚合唯一写入口、状态管理 | N-01～N-05 |
| Attachment Service | 上传、解析、重解析 | N-01, N-08 |
| Knowledge Base Service | 知识库入口管理 | N-05, N-09.1 |

**关键约束：**
- 同一 `taskId` 的写命令必须串行化提交
- 任务入口模式由 `Task Intake Service` 统一判定
- 所有状态变更必须通过 Task Service 提交

### 3.4 编排层

| 服务 | 职责 | 关联需求 |
|------|------|----------|
| Intent Resolver | 复杂任务判定、TaskPlan 生成 | N-02, N-07.5 |
| Plan Builder | 步骤计划生成、子任务拆分 | N-02, N-03 |
| Capability Matcher | Agent/Skill/工具路由匹配 | N-07.2, N-11 |
| Execution Scheduler | 步骤发放、并行控制、等待恢复 | N-03, N-07.7 |
| Run Finalizer | 执行收尾、降级处理 | N-03, N-14 |
| Task Orchestrator | 编排流程协调 | N-02, N-03 |

**关键约束：**
- ReAct 模式执行：推理摘要 → 工具调用 → 观察结果
- 不直接写入任务状态，结果回交 Task Service

### 3.5 领域能力层

| 服务 | 职责 | 关联需求 |
|------|------|----------|
| Input Normalization | 多模态输入标准化 | N-01, N-08 |
| Trigger Evaluation | 触发条件判定、重评估 | N-01, N-14 |
| Trigger Source Adapter | 触发源接入（队列/定时/事件） | N-01, N-14 |
| Agent Execution Service | Agent ReAct 回合执行 | N-03, N-07.5 |
| Knowledge Retrieval | 知识库检索与命中 | N-05, N-09.1 |
| Skill Invocation | Skill 懒加载、工具激活 | N-07.4, N-11 |
| Output Service | 主输出物、结论、报告生成 | N-04, N-05 |
| Tracking & Event | 事件持久化、sequence 管理 | N-14 |
| Knowledge Ingestion | 知识入库、整理 | N-05, N-09.1 |
| Permission Policy | 权限策略合并、决策生成 | N-13 |

### 3.6 基础设施层

| 组件 | 职责 | 技术选型 |
|------|------|----------|
| SQLite | 结构化数据存储 | Drizzle ORM |
| LanceDB | 向量知识检索 | 嵌入式 |
| 文件系统 | 附件、工作区 | 本地 |
| LLM Adapter | 模型统一接入 | OpenAI 兼容 API |
| 日志/审计 | 运行时日志、审计记录 | pino |

## 4. 核心模块分类

### 4.1 模块分类总表

| 分类 | 模块集合 | 核心职责 |
|------|----------|----------|
| **会话与入口** | Conversation Service, Task Intake Service, Trigger Evaluation, Trigger Source Adapter | 任务入口判定、等待态管理 |
| **任务核心** | Task Service, Tracking & Event | 任务生命周期唯一写入口 |
| **编排与执行** | Task Orchestrator, Intent Resolver, Plan Builder, Execution Scheduler, Agent Execution Service | 任务拆解、步骤调度、ReAct执行 |
| **能力支撑** | Skill Invocation, Knowledge Retrieval, Output Service, Permission Policy | 工具、知识、输出、权限 |
| **基础设施** | Input Normalization, Attachment Service, Knowledge Ingestion, LLM Adapter | 输入处理、存储、模型接入 |

### 4.2 会话与入口模块

#### 4.2.1 Conversation Service

- 管理会话生命周期
- 存储用户消息与系统响应
- 提供最近任务引用

#### 4.2.2 Task Intake Service

**职责：**
- 入口模式判定（immediate/queued/scheduled/event_triggered）
- 生成 `TaskTriggerDecision`
- 等待订阅登记与管理
- 触发释放命令提交
- 等待订阅管理 (启动恢复、到期扫描)

**等待调度设计：**
- 扫描间隔：默认5秒
- 批处理大小：每次处理50个订阅
- 扫描超时：单次最长1分钟
- 启动时恢复所有 active 状态订阅

**关键输出：**
- `triggerMode`
- `scheduledAt?`
- `triggerRule?`
- `triggerDecisionSummary`
- `waitingThresholdMode`
- `waitingThresholdBasisSummary`

**入口澄清 (N-01.5)：**
- 当系统对入口模式判定置信度不足时，创建 `clarification_pending` 状态的任务
- 记录判定失败原因、补救动作
- 用户补充信息后，在原 Task 上完成状态推进
- 不得绕过权限裁决
- 待澄清未完成前，不得进入执行链路

#### 4.2.3 Trigger Evaluation Service

**职责：**
- 标准化触发条件判断
- 等待条件重评估
- 异常检测与阈值判定

#### 4.2.4 Trigger Source Adapter

**职责：**
- 队列变更信号接入
- 定时器到期信号接入
- 预定义事件信号接入
- 标准化触发事实输出

### 4.3 任务核心模块

#### 4.3.1 Task Service

**职责：**
- 任务聚合唯一写入口
- 状态变更、事件追加
- 父子任务关系管理
- 双阶段通知控制
- 任务版本冲突处理

**关键约束：**
- 写一致性边界：状态变更 + 事件追加 同提交
- 聚合版本校验防止写冲突
- 一次性释放保障

**版本冲突处理 (N-14)：**
- 使用 `aggregateVersion` 作为写入基线
- aggregateVersion = 时间戳(毫秒) + 序列号
- 写命令携带 `expectedVersion`，与当前版本比较
- 冲突时返回 `TASK_WRITE_CONFLICT` 错误
- 重试策略：最多3次，基础延迟100ms，指数退避，最大延迟2秒
- 重试前重新获取最新版本号
- 写入控制事件：`TaskWriteQueued`、`TaskWriteConflictDetected`

**关键字段：**
- `complexityDecisionSummary`
- `arrangementStatus`
- `estimatedCompletionAt`
- `userNotificationStage`
- `finalOutputReady`
- `outputStage`

#### 4.3.2 Tracking & Event Service

**职责：**
- 事件持久化
- sequence 分配
- 游标管理
- 断线恢复

### 4.4 编排与执行模块

#### 4.4.1 Intent Resolver

**职责：**
- 复杂任务判定
- 生成 `complexityDecisionSummary`
- 入口后任务规划准备

#### 4.4.2 Plan Builder

**职责：**
- 父子任务拆分
- TaskPlan 生成
- Domain Agent 分配
- blocking/non-blocking 标注

**blocking/non-blocking 标注规则 (N-02.3)：**
- 编排阶段必须为每个子任务标注 blocking 或 non-blocking
- 标注一旦用于生成安排通知/ETA/降级交付判定，必须作为可审计事实冻结
- 后续调整必须记录变更事件、原因、影响范围
- 禁止静默改标回溯性修正既有结论

#### 4.4.3 Capability Matcher

**职责：**
- 路由决策
- Agent/Skill/工具匹配
- 执行路径选择

#### 4.4.4 Execution Scheduler

**职责：**
- 步骤发放
- 并行组控制
- 等待/续跑管理
- 全局执行槽位预算控制

#### 4.4.5 Agent Execution Service

**职责：**
- ReAct 回合执行
- 推理摘要生成
- 工具调用执行
- 观察结果标准化

**执行模式：**
```
推理摘要 → 工具调用 → 观察结果 → 下一轮决策 → 终态
```

**超时控制 (N-07.6)：**
- 支持配置 Agent 执行单个步骤的超时时间
- 超时后记录超时事件
- 支持自动重试或进入人工介入状态
- 超时配置支持按 Agent 类型、任务类型分别设置

#### 4.4.6 Run Finalizer

**职责：**
- 执行收尾处理
- 降级判定
- 结果汇总

**降级交付 (N-02.5, N-03.3)：**
- non-blocking 子任务失败时，判定"主要目标是否达成"
- 产出结构化降级交付判定摘要
- 在结果中明确标记差异
- 不把降级误判为主链路失败

### 4.5 能力支撑模块

#### 4.5.1 Skill Invocation Service

**职责：**
- Skill 元数据索引
- 懒加载激活（use_skill）
- 工具注册与约束

**Skill 懒加载方案 (N-11)：**
- 启动阶段只建立 Skill 元数据索引
- `use_skill(skillId)` 是唯一激活入口
- Skill 只在当前 Agent / 当前任务上下文生效
- 任务结束后释放运行时绑定

**Skill 目录结构 (N-11)：**
- Skill 以目录形式存在
- 目录包含 `SKILL.md` 和 `scripts/`
- Agent 通过 Skill ID 引用全局 Skills

**关键约束：**
- 未调用 use_skill 前工具不可用
- Skill 只在当前任务上下文生效

#### 4.5.2 Knowledge Retrieval Service

**职责：**
- 知识库检索
- 命中结果聚合
- 引用关系生成

#### 4.5.3 Output Service

**职责：**
- 主输出物生成
- 结构化结论
- 执行报告
- 结果解释片段

**双阶段输出：**
- 阶段一：`arrangementNoticeSummary` + `outputStage=arranged_only`
- 阶段二：最终输出 + `finalOutputReady=true`

**补充更新通知 (N-04.4)：**
- non-blocking 子任务晚到成功时，只允许补充更新
- 不得静默覆盖既有最终输出
- 补充通知语义：`supplementalNotificationType`
- 与首次最终完成通知严格区分

#### 4.5.4 Permission Policy Service

**职责：**
- 多层策略合并
- 权限决策生成
- 判定轨迹记录

**策略层级（5层）：**
1. 全局默认
2. Agent 配置
3. Skill 绑定
4. 工具级
5. 资源级

**决策优先级：** deny > ask > allow

#### 4.5.5 Prompt Management Service

**职责：**
- Prompt 模板管理（Leader/Domain/Output）
- Prompt 版本控制
- Prompt 超长裁剪

**Prompt 结构：**
- system / developer / user / context / tool-result 五层
- 输出映射：TaskPlan / TaskStepResult / TaskOutput

**裁剪策略 (N-12)：**
- 预估上下文窗口与输出保留预算
- 裁剪顺序：旧步骤结果 → 知识命中 → 长文本输入 → 降级执行
- 裁剪失败：写入 `PROMPT_OVERFLOW` 并触发降级

### 4.6 基础设施模块

#### 4.6.1 Input Normalization Service

- 多模态输入标准化
- TaskInput 生成

#### 4.6.2 Attachment Service

- 附件上传/存储
- 解析/重解析
- 解析状态管理
- **单个附件大小上限 50MB**

#### 4.6.3 Knowledge Ingestion Service

- 知识入库
- 定时整理
- 记忆沉淀

#### 4.6.4 LLM Adapter

- 统一模型接口
- 超时/重试控制
- 供应商切换
- 错误归一化

**错误归一化 (N-07.8)：**
- 归一化错误码：MODEL_TIMEOUT, MODEL_RATE_LIMITED, MODEL_PROVIDER_UNAVAILABLE, MODEL_RESPONSE_INVALID
- 失败摘要标准化输出
- 供应商切换仅适用于可恢复故障 (超时/限流/不可用)
- 保留原供应商失败事实，记录 fallbackProvider

## 5. 模块依赖关系

### 5.1 依赖拓扑

```
接入层
   │
   ▼
应用层 ─────────────────────────────────────────┐
   │                                            │
   ├── Conversation Service ──────► Task Service
   │                                            │
   ├── Task Intake Service ────────► Task Service
   │       │                                    │
   │       ├──► Trigger Evaluation Service     │
   │       │        │                           │
   │       │        └──► Trigger Source Adapter│
   │       │                                    │
   │       └──► Task Service ◄─────────────────┼──► 编排层
   │                                            │
   ├── Attachment Service ──────────────────────┤
   │                                            │
   └── Knowledge Base Service ──────────────────┘
                                                   │
                                                   ▼
编排层 ◄──────────────────────────────────────────┘
   │
   ├── Intent Resolver ──────► Task Service
   ├── Plan Builder ─────────► Task Service
   ├── Capability Matcher ───► Skill Invocation / Knowledge Retrieval
   ├── Execution Scheduler ─► Task Service
   ├── Agent Execution Service ─► Skill Invocation / Knowledge Retrieval / Permission Policy
   └── Run Finalizer ────────► Output Service ◄── Task Service

领域能力层 ◄───────────────────────────────────────┘
   │
   ├── Input Normalization Service
   ├── Trigger Evaluation Service ◄── Trigger Source Adapter
   ├── Agent Execution Service
   ├── Knowledge Retrieval Service
   ├── Skill Invocation Service
   ├── Output Service
   ├── Tracking & Event Service ◄── Task Service
   ├── Knowledge Ingestion Service
   └── Permission Policy Service

基础设施层 ◄──────────────────────────────────────┘
   │
   ├── SQLite ◄── Task Service / Tracking & Event Service
   ├── LanceDB ◄── Knowledge Retrieval Service / Knowledge Ingestion Service
   ├── 文件系统 ◄── Attachment Service / Skill Invocation Service
   └── LLM Adapter ◄── Agent Execution Service
```

### 5.2 关键依赖规则

| 依赖方向 | 规则 |
|----------|------|
| Task Service 是唯一写入口 | 所有模块的状态变更必须回交 Task Service |
| 编排层不直接写状态 | 执行结果回交 Task Service 提交 |
| 权限策略统一调用 | Permission Policy Service 提供统一决策接口 |
| 模型访问统一入口 | LLM Adapter 是唯一模型依赖 |

## 6. 核心数据流

```text
用户输入文本 / 附件
  -> 会话服务写入消息
  -> 附件服务登记并解析
  -> 输入标准化服务生成 TaskInput
  -> Task Intake Service 调用 Trigger Evaluation Service 生成 TaskTriggerDecision
  -> 任务服务创建 Leader 父任务并落账入口模式、`triggerDecisionSummary`、`waitingThresholdMode`、`waitingThresholdBasisSummary` 与等待状态
  -> 若父任务处于 queued / scheduled / event_triggered，则由 Task Intake Service 维护父任务等待态并订阅下一次释放条件
  -> Trigger Source Adapter 接入队列变更、定时器到期或预定义事件事实
  -> Trigger Evaluation Service 重评估等待条件并生成 `lastTriggerEvaluationSummary`、释放结论及必要的等待异常事实
  -> 条件满足后，Task Intake Service 以一次性认领语义向 Task Service 提交父任务进入执行链路的释放命令
  -> 记忆选择与记忆加载范围确认
  -> 编排服务生成父任务级 TaskPlan、`complexityDecisionSummary`、拆分 Domain 子任务并产出路由决策
  -> 任务服务创建 Domain 子任务，分别写入 `parentTaskId`、目标 `assignedDomainAgentId`、子任务入口模式与等待事实
  -> 对 `queued` 子任务写入对应 Domain Agent 显式队列；对 `event_triggered` / `scheduled` 子任务登记各自触发订阅
  -> 各 Domain Agent 返回“已入队 / 已登记定时 / 已登记触发”等安排确认
  -> Task Service 聚合全部子任务安排结果，生成父任务级 ETA、`arrangementStatus`、`arrangementSummary` 与阶段性通知状态
  -> Leader Agent 向用户发送“任务已安排好、预计完成时间”通知，并释放前台交互线程
  -> Execution Scheduler 发放步骤并维护等待/续跑
  -> 生成路由决策摘要与并行执行计划
  -> 权限策略合并判定与判定轨迹生成
  -> 各 Domain Agent 的 Agent / Skill / Knowledge / Tool 执行结果分别回交对应子任务 Task Service
  -> 子任务完成后把结果、耗时和失败信息回传父任务聚合链路
  -> 当全部必需子任务完成后，输出模块基于子任务结果生成父任务级 TaskOutput、ExecutionReport、`outputStage`、`finalOutputReady` 和结果解释片段等事实性结果
  -> 输出结果作为父任务最终步骤结果回交 Task Service
  -> Task Service 提交父任务与子任务状态变更、输出引用、`userNotificationStage` 更新与事件追加
  -> Leader Agent 向用户发送最终结果通知
  -> 任务查询投影汇总任务状态、复杂任务判定摘要、等待异常摘要、阈值依据、时间线游标、知识命中、记忆读写、权限摘要、模型调用摘要与输出解释
  -> 前端展示任务详情与结果页
```

概要层的数据流顺序必须满足两条约束：第一，任务入口判定先于任务创建，并以正式事实写入任务聚合，不能由前端直接决定，也不能等到 TaskPlan 生成后再回填；第二，输出属于最终提交前的步骤结果，且先产出事实，再聚合读模型。也就是说，`TaskTriggerDecision` 先由入口判定链路产出并通过 Task Service 写入任务与入口等待事件；输出模块则在任务完成判定前生成主输出物、执行报告和结果解释片段等事实性结果，并将其回交 Task Service 作为最终步骤结果的一部分；随后由 Task Service 在同一任务提交边界内写入任务状态、输出引用和正式时间线事件，任务查询投影再基于这些事实、任务当前状态和正式时间线事件聚合页面字段。不允许结果页先依赖任务查询投影临时拼装输出事实，再反向写回输出模块。

对 `queued`、`scheduled` 和 `event_triggered` 任务，入口上下文快照在任务创建时即被冻结并成为后续释放执行的基线事实；等待期间允许重评估释放条件，但不允许隐式重算创建时的记忆范围和入口解释，避免同一任务因时间推移读到不可审计的不同上下文。若冻结快照中的部分记忆条目失效，系统只能在保留原快照与失效标记的前提下按既定降级规则继续执行，或进入 `TaskInterventionRequired` 等明确终态，不允许静默切换到释放时刻的新记忆集合。

等待态任务的释放必须满足“一次订阅、一次成功释放”约束：等待订阅可被重复检查、重复命中或在恢复过程中被重复扫描，但只有首次成功认领的释放动作可以把任务推进到 `TaskReadyForPlanning`；后续重复释放尝试只能被识别为幂等命中或冲突，不得重复推进任务主状态。对延迟任务的成功释放，最小正式事件序列也必须固定为 `TaskTriggerActivated -> TaskReadyForPlanning -> MemoryScopeConfirmed`；其中 `TaskTriggerActivated` 不得在成功释放场景下省略，`TaskTriggerReevaluated` 仅用于未释放但条件被重新判断的场景。

等待订阅还必须与任务生命周期保持一致：若任务被取消、超时、失败终止或被人工关闭，则对应等待订阅必须同步失效，不得继续接收触发源事件或在后续恢复扫描中重新释放已终止任务；该失效动作必须与 `TaskCancelled`、`TaskTimedOut`、`TaskFailed` 或 `TaskManuallyClosed` 之一同提交写入。

队列释放策略在概要层固定为 FIFO，因此 `queued` 子任务在所属 Domain Agent 队列中的释放顺序必须与该队列内的订阅建立顺序一致；取消子任务后其队列位置应被移除并允许后续同 Agent 子任务顺位前移，但不得改变尚未取消任务之间的相对顺序。P0 阶段的实际执行释放还受全局任务执行槽位预算影响，但不得绕过 Domain Agent 队列顺序。

对于“先安排、后完成”的业务场景，父任务必须支持两次面向用户的正式反馈：第一次反馈发生在全部必需子任务都完成安排确认之后，并携带聚合 ETA；第二次反馈发生在全部必需子任务完成并汇总出父任务结果之后。父任务的 ETA 可以按各 Domain Agent 当前队列位置、历史平均耗时和已知触发时间综合估算，但其估算过程必须形成可追踪摘要。

除任务主链外，系统还存在一条后台定时记忆整理链路：调度器按日读取会话模块中的当天会话记录，交由知识与记忆能力完成无关信息过滤、摘要提炼和持久记忆写入；该链路不直接改写任务时间线，但必须保留可追溯的记忆来源与整理日志。

## 7. 核心事件流

主生命周期事件：

```text
TaskCreated
  -> ClarificationRequested (N-01.5 低置信度入口)
  -> ClarificationProvided / ClarificationTimeout / ClarificationClosed
  -> TaskTriggerResolved
  -> TaskTriggerWaiting / TaskReadyForPlanning
  -> MemoryScopeConfirmed
  -> TaskPlanned
  -> SubTasksCreated / DomainTaskQueued / DomainTaskScheduled / DomainTaskTriggered
  -> TaskArrangementCompleted / TaskEtaEstimated
  -> RouteResolved
  -> StepStarted
  -> TaskCompleted / TaskPartialFailed / TaskFailed / TaskCancelled / TaskTimedOut / TaskManuallyClosed / TaskInterventionRequired
```

**入口澄清事件 (N-01.5)：**

| 事件名 | 时机 | 关键字段 |
|--------|------|----------|
| ClarificationRequested | 进入澄清待确认状态 | taskId, clarificationFields[], lowConfidenceReason |
| ClarificationProvided | 用户补充信息完成澄清 | taskId, providedInputs[], resolutionSummary |
| ClarificationTimeout | 澄清超时用户未响应 | taskId, timeoutAt, fallbackDecision |
| ClarificationClosed | 用户放弃澄清 | taskId, closeReason |

条件触发事件：

```
记忆与上下文：MemoryLoaded / MemoryRead / MemoryWritten
入口等待：TaskTriggerReevaluated / TaskTriggerActivated / TaskTriggerInvalidated
写入控制：TaskWriteQueued / TaskWriteConflictDetected
权限控制：PermissionCheckRequested / PermissionGranted / PermissionDenied
工具调用：ToolInvoked / ToolCompleted / ToolFailed (N-13.6 审计日志)
并行与等待：StepParallelQueued / StepWaiting
模型调用：ModelCallStarted / ModelCallCompleted / ModelCallFailed
恢复与降级：TaskAutoRetried / TaskDegraded
```

概要层对外公开的正式事件集以任务时间线与事件恢复链路为准，统一采用 `TaskCreated`、`TaskTriggerResolved`、`TaskTriggerWaiting`、`TaskReadyForPlanning`、`TaskPlanned`、`SubTasksCreated`、`DomainTaskQueued`、`DomainTaskScheduled`、`DomainTaskTriggered`、`TaskArrangementCompleted`、`TaskEtaEstimated`、`TaskWriteQueued`、`TaskWriteConflictDetected`、`StepStarted`、`TaskCompleted`、`TaskPartialFailed`、`TaskFailed`、`TaskCancelled`、`TaskTimedOut`、`TaskManuallyClosed`、`TaskInterventionRequired`、`RouteResolved`、`PermissionCheckRequested`、`PermissionGranted`、`PermissionDenied`、`StepParallelQueued`、`StepWaiting`、`TaskTriggerReevaluated`、`TaskTriggerActivated`、`TaskTriggerInvalidated`、`TaskAutoRetried`、`TaskDegraded`、`MemoryScopeConfirmed`、`MemoryLoaded`、`MemoryRead`、`MemoryWritten`、`ModelCallStarted`、`ModelCallCompleted`、`ModelCallFailed`；附件解析、知识检索、Skill 调用、Agent 执行和输出生成等内部处理动作如需落日志，可作为模块内部事件或审计事实存在，但不替代对外统一的任务时间线事件契约。

主生命周期事件用于表达任务从创建、入口模式判定、等待/释放、规划、安排、执行到完成或终止的稳定阶段；其中 `TaskTriggerResolved` 表示系统已经给出入口判定结果，`TaskTriggerWaiting` 表示任务已进入入口等待态，`TaskReadyForPlanning` 表示任务已满足进入执行链路的前置条件，`MemoryScopeConfirmed` 则是任务进入可规划阶段后的基线事件，即使使用默认记忆范围也必须产生日志事件，确保后续审计、恢复和结果解释可以基于统一起点工作。对多 Agent 场景，`SubTasksCreated` 表示父任务已完成子任务拆分，`DomainTaskQueued`、`DomainTaskScheduled`、`DomainTaskTriggered` 表示子任务已完成目标 Domain Agent 的安排确认并进入对应入口控制状态，`TaskArrangementCompleted` 表示全部必需子任务都已安排完成，`TaskEtaEstimated` 表示父任务已形成可对外展示的聚合预计完成时间。`TaskCancelled`、`TaskTimedOut`、`TaskManuallyClosed` 和 `TaskInterventionRequired` 用于表达未完成执行但已被明确终止或挂起等待人工介入的终态；其中 `TaskInterventionRequired` 专用于冻结快照降级后仍不满足最小执行上下文、且系统不能自动继续执行的场景，不等同于人工已执行关闭动作。对成功释放的延迟任务，`TaskTriggerActivated` 必须作为 `TaskReadyForPlanning` 之前的正式事件出现，用于把释放成功与后续规划起点稳定关联起来。条件触发事件用于表达入口等待重评估、入口条件激活、入口订阅失效、写冲突、权限确认、并行等待、模型调用、记忆读写、自动重试和降级等运行时横切行为，它们可以出现多次或穿插在主生命周期事件之间，但不得违反前述成功释放最小事件序列、子任务安排完成语义与同一 `sequence/cursor` 排序恢复规则。

每日定时记忆整理不属于单任务正式时间线事件的一部分；它作为后台作业运行，其产出应通过持久记忆来源字段和作业日志体现，而不是伪装成某个任务的生命周期阶段。

事件流在概要层统一约束为“单任务内严格递增排序 + 游标恢复”模型：所有事件必须分配稳定 `sequence`，前端和应用层以事件游标恢复历史与增量数据，不以时间戳作为唯一排序依据。

在概要层，事件流追加必须从属于任务写入事务边界：`ExecutionEvent` 不是独立于任务聚合的旁路日志，而是由 Task Service 在同一次提交中驱动 Tracking & Event Service 完成持久化的事实记录。

## 8. 系统运行边界

**P0 边界约束：**
| 约束项 | 说明 |
|--------|------|
| 架构选择 | 单体架构，不引入微服务 |
| 部署策略 | 本地优先，不构建复杂云端控制平面 |
| 用户规模 | 单用户/小团队，不构建多租户体系 |
| 设计原则 | Prompt、Skills、工具和知识库必须围绕任务主链路设计 |

**技术边界：**
- 单体部署。
- 本地文件系统。
- 本地数据库和向量索引。
- 通过外部模型 API 获得推理能力。

系统对外部模型 API 的访问统一经由 `LLM Adapter`，并在该层完成超时、重试、熔断、错误归一化和降级出口控制；概要层不允许业务模块直接与具体模型供应商建立耦合。

**P0 不纳入范围 (与02-功能要求对齐)：**
| 序号 | 不纳入功能 |
|------|-----------|
| 1 | 多租户与复杂权限系统 |
| 2 | DAG 工作流编排器 |
| 3 | 音频、视频输入 |
| 4 | 图像生成、语音合成等输出型多模态能力 |
| 5 | 微服务拆分、分布式任务队列、复杂消息中间件 |

## 9. 非功能分析

### 8.1 可用性

- 任务执行过程可见。
- 失败后可重试。
- 结果与过程分离展示。
- 路由决策、并行状态、记忆使用和降级处理对用户可见且可理解。

### 8.2 性能

- 长任务采用事件流和分阶段查询。
- Prompt 和输入上下文必须支持裁剪。

### 8.3 安全性

- 工具访问必须经过沙箱。
- 敏感配置最小暴露。
- Skills 和高权限工具必须懒加载和按需激活。
- 写入、编辑、执行和外部系统交互必须经过统一权限策略判断，并按 `deny > ask > allow` 的最严格优先级生成默认执行、待权限确认或拒绝执行等权限决策。

**沙箱执行规范 (N-13)：**
- 限制工作目录，限制访问路径
- 命令执行必须超时控制
- 输出必须支持截断标记和摘要化
- 写入、编辑、执行和外部系统调用必须在沙箱外再叠加权限策略检查
- 当权限策略为 `ask` 时，需要先生成权限请求描述，再等待用户确认
- 权限请求需包含：动作类型、发起Agent、关联Skill、目标资源、潜在影响和当前权限策略
- 权限判定轨迹需记录：每一层输入策略、最终决策、命中规则、资源范围

**凭证安全管理 (N-16.1)：**
- API Keys、数据库密码加密存储（AES-256-GCM）
- 凭证操作需审计记录
- 支持凭证轮换

**数据脱敏 (N-16.2)：**
- 身份证号、手机号、银行卡等敏感信息脱敏
- 脱敏规则可配置
- 导出报告自动脱敏

**审计日志 (N-16.3)：**
- 保留至少 90 天
- 包含操作人、时间、类型、影响范围
- 支持导出

**网络通信安全 (N-16.4)：**
- HTTPS/TLS 加密
- Token 认证
- 本地进程间通信加密

**工具安全防护 (N-13.5)：**
- 所有工具调用参数先进行安全扫描，再执行脚本注入过滤
- 命令注入过滤：移除 `;`, `|`, `&&`, `$()`, `` ` ``, `$(())` 等 Shell 元字符
- 路径遍历过滤：阻止 `../` 逃逸，验证最终路径在工作目录内
- 环境变量注入过滤：禁止 `${VAR}` 或 `$VAR` 形式的未授权变量展开
- 写入操作安全：
  - 禁止写入可执行文件 (.exe, .sh, .bat, .ps1, .py 等)
  - 禁止写入包含恶意代码的内容
  - 工具可生成可执行文件，但执行前需额外安全检查和权限确认

**任务恢复机制 (N-14.2)：**
- 系统重启后自动检查并恢复处于执行中或等待态的中断任务
- 任务恢复后继续执行而非重新开始
- 恢复时重建等待订阅，按 `scheduledAt / nextTriggerCheckAt` 重建调度

所有 Agent 仅能通过受控的 Service 与工具和外部环境交互，其权限由配置和 Skill 绑定的白名单统一管理，不允许直接绕过服务层访问底层资源或提升自身权限。

## 9.1 P1 功能扩展

> 以下为 P1 阶段功能，本概要设计仅作预留说明，详细设计在后续版本补充。

**N-10 自我学习与经验积累：**
- 任务模板管理
- 会话日摘要作为经验沉淀
- 经验提取与主动建议

**N-15 监控告警机制：**
- 系统健康指标采集（任务成功率、平均耗时、模型调用成功率等）
- 异常检测与预警规则
- 分级告警与通知策略
- 监控面板与运营视图

**N-10A Agent 自我反思能力：**
- 输出质量自检
- 执行路径复盘
- 推理一致性校验
- 经验提取与主动建议

**N-17 运维与部署：**
- 健康检查接口
- 配置热更新
- 数据备份与恢复
- 资源监控

## 10. 数据模型设计

### 10.1 核心实体关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              核心实体关系图                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌──────────────┐         ┌──────────────┐         ┌──────────────┐      │
│   │  Conversation │◄──────►│    Task      │◄──────►│  TaskStep    │      │
│   │   会话        │    1:N │    任务       │    1:N │    步骤       │      │
│   └──────────────┘         └──────────────┘         └──────────────┘      │
│         │                        │                        │                  │
│         │                        │                        │                  │
│         ▼                        ▼                        ▼                  │
│   ┌──────────────┐         ┌──────────────┐         ┌──────────────┐      │
│   │   Message    │         │    Agent     │         │   Output     │      │
│   │    消息       │         │    Agent     │         │    输出       │      │
│   └──────────────┘         └──────────────┘         └──────────────┘      │
│                                    │                        │                  │
│                                    │         ┌──────────────┐              │
│                                    ├────────►│  Execution   │              │
│                                    │    N:1  │   Event      │              │
│                                    │         │   执行事件    │              │
│                                    │         └──────────────┘              │
│                                    │                        │                  │
│                                    ▼                        ▼                  │
│                           ┌──────────────┐         ┌──────────────┐      │
│                           │  DomainAgent │         │   OutputRef  │      │
│                           │   领域Agent  │         │   输出引用   │      │
│                           └──────────────┘         └──────────────┘      │
│                                                                             │
│   ┌──────────────┐         ┌──────────────┐                               │
│   │  Attachment  │◄──────►│  Knowledge   │                               │
│   │    附件       │    N:N │    知识       │                               │
│   └──────────────┘         └──────────────┘                               │
│                                                                             │
│   ┌──────────────┐         ┌──────────────┐                               │
│   │   Memory     │◄──────►│    Skill     │                               │
│   │    记忆       │    N:N │    Skill     │                               │
│   └──────────────┘         └──────────────┘                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 10.2 核心实体定义

#### 10.2.1 Conversation (会话)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 会话唯一标识 (UUID) |
| title | string | 会话标题 |
| createdAt | timestamp | 创建时间 |
| updatedAt | timestamp | 更新时间 |
| lastActiveAt | timestamp | 最后活跃时间 |
| status | enum | active/archived |

**关联关系：**
- 1:N Message
- N:1 Task (通过 lastActiveTaskId)

#### 10.2.2 Message (消息)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 消息唯一标识 |
| conversationId | string | 所属会话 |
| entryPoint | string | 接入端来源 (web/cli/api等) |
| originClientId | string? | 原始客户端标识 |
| role | enum | user/assistant/system |
| content | text | 消息内容 |
| attachments | json | 附件引用列表 |
| createdAt | timestamp | 创建时间 |

**关联关系：**
- N:1 Conversation

**多端区分说明：**
- `entryPoint` 用于标识消息来自哪个接入端类型，例如 `web`、`desktop`、`mobile`、`cli`、`api`。
- `originClientId` 用于标识具体发起端实例，例如某个浏览器标签页、某个桌面客户端实例或某台移动设备。
- 多端支持通过 `entryPoint + originClientId` 这组字段在消息和任务层面进行区分，而不是依赖多会话模型来区分不同端。

#### 10.2.3 Task (任务)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 任务唯一标识 |
| parentTaskId | string? | 父任务ID (子任务有值) |
| conversationId | string | 关联会话 |
| entryPoint | string | 接入端来源 (web/cli/api等) |
| originClientId | string? | 原始客户端标识 |
| triggerMode | enum | immediate/queued/scheduled/event_triggered/clarification_pending |
| triggerDecisionSummary | text | 入口判定摘要 |
| complexityDecisionSummary | text | 复杂任务判定摘要 |
| complexity | enum | simple/complex |
| status | enum | pending/planning/arranged/running/completed/failed/cancelled |
| arrangementStatus | enum? | waiting_for_arrangement/arranged |
| userNotificationStage | enum | none/arranged/final_notified |

**多端显示说明：**
- `entryPoint`、`originClientId` 用于表达该任务最初由哪个接入端、哪个具体客户端实例发起。
- 任务的跨端显示与同步不只看任务本身，还需要结合接口和事件载荷中的 `syncPolicy`、`visibleClientIds`、`displayScope` 一起判断。
- 设计目标是：每个接入端都采用同一主交互模型，但是否把同一轮输入、进度、结果和权限信息同步到其他端，由同步策略决定。
| outputStage | enum | none/arranged_only/final |
| finalOutputReady | boolean | 最终输出是否就绪 |
| estimatedCompletionAt | timestamp? | 预计完成时间 |
| estimatedDurationMinutes | int? | 预计耗时(分钟) |
| terminalSummary | text? | 终态摘要 (无输出时) |
| closeReason | text? | 关闭/终止原因 |
| createdAt | timestamp | 创建时间 |
| updatedAt | timestamp | 更新时间 |
| completedAt | timestamp? | 完成时间 |

**子任务附加字段 (N-02.3)：**
| 字段 | 类型 | 说明 |
|------|------|------|
| assignedDomainAgentId | string? | 分配的Domain Agent |
| blocking | enum? | blocking/non-blocking/null (父任务无此字段) |
| queuePosition | int? | 队列位置 (queued类型) |

**关联关系：**
- N:1 Conversation
- N:1 Task (parent)
- N:N Agent (through TaskAgent)
- N:N Output
- N:N Attachment
- 1:N TaskStep

#### 10.2.4 TaskStep (步骤)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 步骤唯一标识 |
| taskId | string | 所属任务 |
| agentId | string | 执行Agent |
| stepOrder | int | 步骤序号 |
| name | string | 步骤名称 |
| status | enum | pending/running/waiting/completed/failed |
| reasoningSummary | text | 推理摘要 |
| actionSummary | text | 动作摘要 |
| observationSummary | text | 观察摘要 |
| parallelGroupId | string? | 并行组ID |
| startedAt | timestamp? | 开始时间 |
| completedAt | timestamp? | 完成时间 |
| duration | int | 执行耗时(ms) |

**关联关系：**
- N:1 Task
- N:1 Agent

#### 10.2.5 Agent (Agent)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | Agent唯一标识 |
| type | enum | leader/domain |
| name | string | Agent名称 |
| role | text | 角色描述 |
| model | string | 使用模型 |
| temperature | float | 温度参数 |
| skills | string[] | 绑定Skills |
| knowledgeBases | string[] | 知识库开关 |
| status | enum | active/inactive |
| isSystem | boolean | 是否系统内置 |
| createdAt | timestamp | 创建时间 |
| updatedAt | timestamp | 更新时间 |

**关联关系：**
- N:N Skill
- N:N KnowledgeBase

#### 10.2.6 Output (输出)

| 字段 | 类型 |说明 |
|------|------|------|
| id | string | 输出唯一标识 |
| taskId | string | 关联任务 |
| type | enum | final/intermediate/arrangement |
| content | text | 输出内容 |
| summary | text | 摘要 |
| references | json | 引用关系 |
| createdAt | timestamp | 创建时间 |

**关联关系：**
- N:1 Task

#### 10.2.7 ExecutionEvent (执行事件)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 事件唯一标识 |
| taskId | string | 关联任务 |
| sequence | int | 任务内递增序号 |
| eventType | string | 事件类型 |
| timestamp | timestamp | 事件时间 |
| summary | text | 事件摘要 |
| payload | json | 事件数据 |
| stepId | string? | 关联步骤 |
| agentId | string? | 关联Agent |

**关联关系：**
- N:1 Task
- N:1 TaskStep (optional)
- N:1 Agent (optional)

#### 10.2.8 Attachment (附件)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 附件唯一标识 |
| taskId | string? | 关联任务 |
| conversationId | string? | 关联会话 |
| fileName | string | 文件名 |
| fileType | enum | image/pdf/doc |
| fileSize | int | 文件大小 |
| storagePath | string | 存储路径 |
| parseStatus | enum | pending/success/failed |
| parseSummary | text | 解析摘要 |
| createdAt | timestamp | 上传时间 |

#### 10.2.9 Skill (Skill)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | Skill唯一标识 |
| name | string | Skill名称 |
| description | text | Skill描述 |
| status | enum | active/inactive |
| permissions | json | 权限清单 |
| tools | json | 工具定义 |
| instructions | text | 指令模板 |
| createdAt | timestamp | 创建时间 |
| updatedAt | timestamp | 更新时间 |

#### 10.2.10 Knowledge (知识)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 知识唯一标识 |
| title | string | 知识标题 |
| content | text | 知识内容 |
| vectorIds | string[] | 向量ID列表 |
| sourceType | enum | user_import/task_output/manual |
| sourceTaskId | string? | 来源任务 |
| agentId | string? | 关联Agent |
| createdAt | timestamp | 创建时间 |
| updatedAt | timestamp | 更新时间 |

#### 10.2.11 Memory (记忆)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 记忆唯一标识 |
| agentId | string? | 关联Agent (持久记忆) |
| conversationId | string? | 关联会话 (会话记忆) |
| taskId | string? | 关联任务 (任务记忆) |
| type | enum | short_term/agent/persistent |
| content | text | 记忆内容 |
| summary | text | 摘要 |
| sourceType | enum | task_output/manual/daily_digest |
| importance | enum | low/medium/high |
| createdAt | timestamp | 创建时间 |
| expiresAt | timestamp? | 过期时间 |

#### 10.2.12 WaitSubscription (等待订阅)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 订阅唯一标识 |
| taskId | string | 关联任务 |
| type | enum | queued/scheduled/event_triggered |
| domainAgentId | string? | 目标Agent (queued) |
| queuePosition | int? | 队列位置 |
| scheduledAt | timestamp? | 计划时间 |
| triggerRule | json? | 触发规则 |
| nextCheckAt | timestamp | 下次检查时间 |
| lastEvaluatedAt | timestamp? | 最近评估时间 |
| thresholdConfig | json | 阈值配置 |
| status | enum | active/released/cancelled |
| createdAt | timestamp | 创建时间 |

### 10.3 父子任务关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            父子任务关系模型                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐  │
│   │                        Leader 父任务                                 │  │
│   │  ┌─────────────────────────────────────────────────────────────┐   │  │
│   │  │ taskId: task-001                                            │   │  │
│   │  │ type: leader                                                │   │  │
│   │  │ status: running                                            │   │  │
│   │  │ arrangementStatus: arranged                                 │   │  │
│   │  │ userNotificationStage: arranged                            │   │  │
│   │  └─────────────────────────────────────────────────────────────┘   │  │
│   │                              │                                        │  │
│   │         ┌────────────────────┼────────────────────┐                 │  │
│   │         │                    │                    │                 │  │
│   │         ▼                    ▼                    ▼                 │  │
│   │  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐           │  │
│   │  │ Domain 子任务 │     │ Domain 子任务 │     │ Domain 子任务 │           │  │
│   │  │  task-002   │     │  task-003   │     │  task-004   │           │  │
│   │  │ type: domain │     │ type: domain │     │ type: domain │           │  │
│   │  │ agent: 代码  │     │ agent: 文档  │     │ agent: 数据  │           │  │
│   │  │ blocking: ✓ │     │ blocking: ✓ │     │ blocking: ✗ │           │  │
│   │  │ status: ✓  │     │ status: ✓  │     │ status: ✗  │           │  │
│   │  └─────────────┘     └─────────────┘     └─────────────┘           │  │
│   │        │                    │                    │                   │  │
│   │        └────────────────────┼────────────────────┘                 │  │
│   │                             ▼                                        │  │
│   │                    ┌─────────────┐                                  │  │
│   │                    │  结果汇总    │                                  │  │
│   │                    │ outputStage │                                  │  │
│   │                    │ = final     │                                  │  │
│   │                    └─────────────┘                                  │  │
│   └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 11. API 接口设计

### 11.1 API 分组

| 分组 | 前缀 | 说明 |
|------|------|------|
| 会话 | /api/conversation | 默认会话上下文与消息交互 |
| 消息 | /api/conversation/messages | 默认会话消息发送/接收 |
| 任务 | /api/tasks | 任务CRUD |
| 步骤 | /api/tasks/:id/steps | 步骤查询 |
| Agent | /api/agents | Agent管理 |
| Skill | /api/skills | Skill管理 |
| 知识库 | /api/knowledge | 知识管理 |
| 记忆 | /api/memories | 记忆管理 |
| 附件 | /api/attachments | 附件上传 |
| 配置 | /api/config | 系统配置 |
| 权限 | /api/policies | 权限策略 |
| 健康 | /health | 健康检查 |

### 11.2 核心 API 端点

#### 11.2.0 多端请求上下文

为支持多端接入，API 传输层应显式携带客户端来源信息；服务端在进入应用层后，将这些传输字段统一归一化为 `entryPoint`、`originClientId` 等领域字段。

| 传输位置 | 字段 | 是否必填 | 说明 |
|------|------|------|------|
| Header | `X-Client-Id` | 是 | 具体客户端实例标识；用于区分消息和任务究竟来自哪个端实例，例如浏览器标签页、桌面客户端实例、移动设备实例 |
| Header | `X-Entry-Point` | 是 | 接入端类型标识，例如 `web`、`desktop`、`mobile`、`cli`、`api` |

约束：
- `X-Client-Id` 是 API 设计中直接区分不同端实例的标准字段，不能只停留在落库层或事件载荷层。
- `originClientId`、`entryPoint` 属于服务端归一化后的领域字段，不应作为消息发送、任务创建等业务请求体字段由客户端重复传入。
- 写请求至少需要显式携带 `X-Client-Id` 与 `X-Entry-Point`；读请求若涉及多端显示范围判断，也需携带这些 Header。
- 服务端在持久化 `Message`、`Task` 与事件 `payload` 时，统一写入规范化后的 `originClientId`。

#### 11.2.1 会话 API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /api/conversation | 获取默认会话上下文；请求头需支持 `X-Client-Id` |
| GET | /api/conversation/messages | 获取默认会话消息；请求头需支持 `X-Client-Id` |
| POST | /api/conversation/messages | 发送消息；支持普通任务请求与只读任务查询指令；请求头需显式携带 `X-Client-Id` 与 `X-Entry-Point` |

#### 11.2.2 消息 API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /api/conversation/messages | 获取默认会话消息 |
| POST | /api/conversation/messages | 发送消息 |

#### 11.2.3 任务 API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /api/tasks | 获取任务列表 |
| POST | /api/tasks | 创建任务；请求头需显式携带 `X-Client-Id` 与 `X-Entry-Point` |
| GET | /api/tasks/:id | 获取任务详情；请求头需支持 `X-Client-Id` 以判断多端显示范围 |
| PATCH | /api/tasks/:id | 更新任务 |
| DELETE | /api/tasks/:id | 删除任务 |
| POST | /api/tasks/:id/cancel | 取消任务 |
| POST | /api/tasks/:id/retry | 重试任务 |
| GET | /api/tasks/:id/events | 获取任务事件流；请求头需支持 `X-Client-Id` |
| GET | /api/tasks/:id/steps | 获取任务步骤 |
| GET | /api/tasks/:id/output | 获取任务输出 |

#### 11.2.4 入口判定 API

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | /api/tasks/:id/confirm-trigger | 确认入口模式 |
| POST | /api/tasks/:id/clarify | 补充入口信息 |

#### 11.2.5 权限确认 API

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | /api/permissions/:id/grant | 授予权限 |
| POST | /api/permissions/:id/deny | 拒绝权限 |

#### 11.2.6 Agent API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /api/agents | 获取Agent列表 |
| POST | /api/agents | 创建Agent |
| GET | /api/agents/:id | 获取Agent详情 |
| PATCH | /api/agents/:id | 更新Agent |
| DELETE | /api/agents/:id | 删除Agent |
| POST | /api/agents/:id/test | 测试连通性 |

#### 11.2.7 Skill API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /api/skills | 获取Skill列表 |
| POST | /api/skills | 创建Skill |
| GET | /api/skills/:id | 获取Skill详情 |
| PATCH | /api/skills/:id | 更新Skill |
| DELETE | /api/skills/:id | 删除Skill |
| POST | /api/skills/:id/activate | 激活Skill |

#### 11.2.8 知识库 API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /api/knowledge | 获取知识列表 |
| POST | /api/knowledge | 添加知识 |
| GET | /api/knowledge/:id | 获取知识详情 |
| DELETE | /api/knowledge/:id | 删除知识 |
| POST | /api/knowledge/search | 搜索知识 |

#### 11.2.9 记忆 API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /api/memories | 获取记忆列表 |
| POST | /api/memories | 创建记忆 |
| DELETE | /api/memories/:id | 删除记忆 |
| PATCH | /api/memories/:id | 更新记忆 |

#### 11.2.10 附件 API

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | /api/attachments/upload | 上传附件 |
| GET | /api/attachments/:id | 获取附件信息 |
| DELETE | /api/attachments/:id | 删除附件 |
| POST | /api/attachments/:id/reparse | 重新解析 |

#### 11.2.11 配置 API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /api/config | 获取配置 |
| PATCH | /api/config | 更新配置 |
| GET | /api/config/models | 获取模型配置 |
| POST | /api/config/models/test | 测试模型连通性 |

#### 11.2.12 权限策略 API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /api/policies | 获取权限策略列表 |
| POST | /api/policies | 创建权限策略 |
| GET | /api/policies/:id | 获取策略详情 |
| PATCH | /api/policies/:id | 更新策略 |
| DELETE | /api/policies/:id | 删除策略 |

#### 11.2.13 健康检查 API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /health | 健康检查 |
| GET | /health/ready | 就绪检查 |
| GET | /health/live | 存活检查 |

### 11.3 WebSocket 事件

| 事件名 | 方向 | 说明 |
|--------|------|------|
| task.created | server->client | 任务创建 |
| task.updated | server->client | 任务状态更新 |
| task.completed | server->client | 任务完成 |
| task.failed | server->client | 任务失败 |
| step.started | server->client | 步骤开始 |
| step.completed | server->client | 步骤完成 |
| step.failed | server->client | 步骤失败 |
| permission.requested | server->client | 权限请求 |
| arrangement.completed | server->client | 安排完成 |
| trigger.activated | server->client | 触发激活 |

说明：涉及会话主交互展示或跨端同步的事件，实际事件载荷还应包含 `entryPoint`、`originClientId`、`syncPolicy`、`visibleClientIds`、`displayScope` 或等价字段，用于让不同端判断该事件是否应展示以及如何同步。

### 11.4 错误响应格式

```json
{
  "error": {
    "code": "TASK_NOT_FOUND",
    "message": "任务不存在",
    "details": {}
  }
}
```

### 11.5 分页响应格式

```json
{
  "data": [],
  "pagination": {
    "total": 100,
    "page": 1,
    "pageSize": 20,
    "hasMore": true
  }
}
```

## 12. 配置模型设计

### 12.1 配置分类

| 分类 | 用途 | 热更新 |
|------|------|--------|
| 模型配置 | API Key、Base URL、模型参数 | ✅ |
| Agent配置 | 角色、温度、Skills | ✅ |
| 系统配置 | 超时、日志、并发 | ✅ |
| 权限策略 | allow/ask/deny规则 | ✅ |
| UI配置 | 主题、语言 | ✅ |

### 12.2 模型配置

```typescript
interface ModelConfig {
  provider: 'openai' | 'anthropic' | 'custom';
  apiKey: string;           // 加密存储
  baseUrl: string;
  model: string;
  temperature: number;      // 0-2
  maxTokens: number;
  timeout: number;          // ms
  retry: {
    maxAttempts: number;
    baseDelay: number;
  };
}
```

### 12.3 Agent 配置

```typescript
interface AgentConfig {
  id: string;
  name: string;
  type: 'leader' | 'domain';
  role: string;
  model: string;
  temperature: number;
  skills: string[];          // Skill ID列表
  knowledgeBases: {
    enabled: boolean;
    agentId?: string;
  }[];
  timeout: {
    step: number;           // 步骤超时 ms
    task: number;           // 任务超时 ms
  };
  priority: number;
}
```

### 12.4 系统配置

```typescript
interface SystemConfig {
  server: {
    port: number;
    host: string;
  };
  workspace: {
    path: string;
    maxFileSize: number;    // MB
  };
  task: {
    maxConcurrent: number;
    defaultTimeout: number;
    maxRetries: number;
  };
  queue: {
    scanInterval: number;   // ms
    batchSize: number;
  };
  memory: {
    maxShortTermAge: number; // ms
    autoCleanup: boolean;
  };
  log: {
    level: 'debug' | 'info' | 'warn' | 'error';
  };
}
```

### 12.5 权限策略配置

```typescript
interface PermissionPolicy {
  id: string;
  name: string;
  level: 'global' | 'agent' | 'skill' | 'tool' | 'resource';
  scope: {
    agentId?: string;
    skillId?: string;
    toolId?: string;
    resourcePattern?: string;
  };
  rules: {
    action: 'read' | 'write' | 'execute';
    decision: 'allow' | 'ask' | 'deny';
  }[];
  priority: number;
}
```

### 12.6 策略层级配置示例

```typescript
// 5层策略配置
const policyLayers: PermissionPolicy[] = [
  // Layer 1: 全局默认
  {
    level: 'global',
    rules: [
      { action: 'read', decision: 'allow' },
      { action: 'write', decision: 'ask' },
      { action: 'execute', decision: 'deny' }
    ]
  },
  // Layer 2: Agent配置
  {
    level: 'agent',
    agentId: 'code-agent',
    rules: [
      { action: 'execute', decision: 'allow' }  // 收紧
    ]
  },
  // Layer 3: Skill绑定
  {
    level: 'skill',
    skillId: 'file-skill',
    rules: [
      { action: 'write', decision: 'deny' }  // 收紧
    ]
  },
  // Layer 4: 工具级
  {
    level: 'tool',
    toolId: 'shell.exec',
    rules: [
      { action: 'execute', decision: 'deny' }
    ]
  },
  // Layer 5: 资源级
  {
    level: 'resource',
    resourcePattern: '/workspace/.env',
    rules: [
      { action: 'read', decision: 'deny' }
    ]
  }
];
```

**Ask 模式超时配置 (N-13.14.1)：**
```typescript
interface AskTimeoutConfig {
  defaultTimeout: number;    // 默认 5 分钟 (300000ms)
  maxTimeout: number;       // 最大 30 分钟 (1800000ms)
  autoUpgradeOnTimeout: boolean;  // 超时后自动升级为 deny
  riskBasedTimeout: boolean;      // 高风险操作超时时间短于低风险
}
```

### 12.7 等待阈值配置

```typescript
interface WaitThresholdConfig {
  queued: {
    maxWaitTimeMs: number;
    maxRetryCount: number;
    anomalyThresholdMs: number;
  };
  scheduled: {
    maxWaitTimeMs: number;
    anomalyThresholdMs: number;
  };
  eventTriggered: {
    maxWaitTimeMs: number;
    maxRetryCount: number;
    anomalyThresholdMs: number;
  };
}
```

## 13. 部署架构

### 13.1 单体部署架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           BiosBot 部署架构                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                         客户端层                                     │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │    │
│  │  │  Web UI    │  │  WebSocket  │  │  (Future)   │               │    │
│  │  │  Browser   │  │   Client    │  │  CLI/Mobile │               │    │
│  │  └─────────────┘  └─────────────┘  └─────────────┘               │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                      │                                       │
│                                      ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                         接入层                                       │    │
│  │  ┌─────────────────────────────────────────────────────────────┐    │    │
│  │  │                    Node.js / Express                        │    │    │
│  │  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │    │    │
│  │  │  │  HTTP API   │  │  WebSocket  │  │  /health    │        │    │    │
│  │  │  │  Server     │  │  Server     │  │  Endpoints  │        │    │    │
│  │  │  └─────────────┘  └─────────────┘  └─────────────┘        │    │    │
│  │  └─────────────────────────────────────────────────────────────┘    │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                      │                                       │
│                                      ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                         应用层                                       │    │
│  │  ┌─────────────────────────────────────────────────────────────┐    │    │
│  │  │                    Application Services                      │    │    │
│  │  │  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐   │    │    │
│  │  │  │Conversation   │ │TaskIntake     │ │Task           │   │    │    │
│  │  │  │Service        │ │Service        │ │Service        │   │    │    │
│  │  │  └───────────────┘ └───────────────┘ └───────────────┘   │    │    │
│  │  │  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐   │    │    │
│  │  │  │Attachment     │ │KnowledgeBase  │ │               │   │    │    │
│  │  │  │Service        │ │Service        │ │               │   │    │    │
│  │  │  └───────────────┘ └───────────────┘ └───────────────┘   │    │    │
│  │  └─────────────────────────────────────────────────────────────┘    │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                      │                                       │
│                                      ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                         编排层                                       │    │
│  │  ┌─────────────────────────────────────────────────────────────┐    │    │
│  │  │                    Orchestration Services                   │    │    │
│  │  │  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐   │    │    │
│  │  │  │IntentResolver │ │PlanBuilder    │ │Execution      │   │    │    │
│  │  │  │              │ │              │ │Scheduler      │   │    │    │
│  │  │  └───────────────┘ └───────────────┘ └───────────────┘   │    │    │
│  │  └─────────────────────────────────────────────────────────────┘    │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                      │                                       │
│                                      ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                       领域能力层                                      │    │
│  │  ┌─────────────────────────────────────────────────────────────┐    │    │
│  │  │                    Domain Services                          │    │    │
│  │  │  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐    │    │    │
│  │  │  │AgentExecution │ │Skill          │ │Knowledge      │    │    │    │
│  │  │  │Service        │ │Invocation     │ │Retrieval      │    │    │    │
│  │  │  └───────────────┘ └───────────────┘ └───────────────┘    │    │    │
│  │  │  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐    │    │    │
│  │  │  │Output         │ │Permission     │ │Trigger       │    │    │    │
│  │  │  │Service        │ │PolicyService  │ │Evaluation    │    │    │    │
│  │  │  └───────────────┘ └───────────────┘ └───────────────┘    │    │    │
│  │  └─────────────────────────────────────────────────────────────┘    │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                      │                                       │
│                                      ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                       基础设施层                                      │    │
│  │  ┌─────────────────────────────────────────────────────────────┐    │    │
│  │  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │    │    │
│  │  │  │ SQLite  │  │LanceDB  │  │ File    │  │  LLM    │        │    │    │
│  │  │  │(Drizzle)│  │         │  │ System  │  │ Adapter │        │    │    │
│  │  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │    │    │
│  │  │                                                          │    │    │
│  │  │  ┌─────────────────────────────────────────────────┐    │    │    │
│  │  │  │              工作区 (Workspace)                   │    │    │    │
│  │  │  │  ┌─────────┐  ┌─────────┐  ┌─────────┐        │    │    │    │
│  │  │  │  │上传文件 │  │ 生成文件 │  │ 工具执行 │        │    │    │    │
│  │  │  │  └─────────┘  └─────────┘  └─────────┘        │    │    │    │
│  │  │  └─────────────────────────────────────────────────┘    │    │    │
│  │  └─────────────────────────────────────────────────────────────┘    │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                      │                                       │
│                                      ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                       外部依赖                                        │    │
│  │  ┌─────────────────────────────────────────────────────────────┐    │    │
│  │  │                    LLM Provider APIs                        │    │    │
│  │  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │    │    │
│  │  │  │   OpenAI    │  │  Anthropic  │  │   Custom    │        │    │    │
│  │  │  │   API       │  │   API       │  │   API       │        │    │    │
│  │  │  └─────────────┘  └─────────────┘  └─────────────┘        │    │    │
│  │  └─────────────────────────────────────────────────────────────┘    │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 13.2 进程内组件关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         BiosBot 进程内组件                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                        HTTP / WebSocket Server                       │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                      │                                       │
│                                      ▼                                       │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                         Request Handler                               │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐            │  │
│  │  │Auth      │  │Validate  │  │Rate      │  │Route     │            │  │
│  │  │Middleware│  │Middleware │  │Limit     │  │          │            │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘            │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                      │                                       │
│                                      ▼                                       │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                       Service Container                              │  │
│  │                                                                      │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐│  │
│  │  │                    Application Layer                           ││  │
│  │  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ││  │
│  │  │  │Conversation│ │TaskIntake│ │Task    │ │Attachment│ │Knowledge│ ││  │
│  │  │  │Service   │ │Service  │ │Service  │ │Service  │ │BaseSvc  │ ││  │
│  │  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ ││  │
│  │  └─────────────────────────────────────────────────────────────────┘│  │
│  │                                                                      │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐│  │
│  │  │                    Orchestration Layer                          ││  │
│  │  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ││  │
│  │  │  │Intent   │ │Plan     │ │Capability│ │Scheduler│ │Run      │ ││  │
│  │  │  │Resolver │ │Builder  │ │Matcher  │ │         │ │Finalizer│ ││  │
│  │  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ ││  │
│  │  └─────────────────────────────────────────────────────────────────┘│  │
│  │                                                                      │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐│  │
│  │  │                    Domain Layer                                 ││  │
│  │  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ││  │
│  │  │  │Agent    │ │Skill    │ │Knowledge│ │Output   │ │Permission│ ││  │
│  │  │  │Execution│ │Invocation│ │Retrieval│ │Service  │ │Policy   │ ││  │
│  │  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ ││  │
│  │  └─────────────────────────────────────────────────────────────────┘│  │
│  │                                                                      │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                      │                                       │
│                                      ▼                                       │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                         Infrastructure                              │  │
│  │                                                                      │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐            │  │
│  │  │Database  │  │Vector DB │  │File     │  │LLM       │            │  │
│  │  │(SQLite)  │  │(LanceDB) │  │Storage  │  │Adapter   │            │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘            │  │
│  │                                                                      │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐            │  │
│  │  │Scheduler │  │Event     │  │Sandbox   │  │Logger    │            │  │
│  │  │(Cron)    │  │Bus       │  │(Tools)   │  │(pino)    │            │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘            │  │
│  │                                                                      │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 13.3 数据流与组件协作

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         请求处理流程                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. HTTP Request                                                           │
│       │                                                                     │
│       ▼                                                                     │
│  2. Middleware (Auth, Validate, RateLimit)                                  │
│       │                                                                     │
│       ▼                                                                     │
│  3. Route Handler                                                           │
│       │                                                                     │
│       ├──► Conversation Service ──► Database                                │
│       │                                                                     │
│       ├──► Task Intake Service ──► Trigger Evaluation ──► Task Service      │
│       │                                              │                      │
│       │                                              ▼                      │
│       │                                        Database                     │
│       │                                                                     │
│       ├──► Attachment Service ──► File Storage ──► Parser                  │
│       │                                                                     │
│       ├──► Knowledge Service ──► Vector DB                                 │
│       │                                                                     │
│       ├──► Agent Service ──► LLM Adapter ──► External API                   │
│       │                                                                     │
│       └──► WebSocket ──► Push to Client                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 13.4 任务执行流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         任务执行流程                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Task Created                                                               │
│       │                                                                     │
│       ▼                                                                     │
│  ┌─────────────────────────┐                                               │
│  │  Entry Decision         │  Task Intake Service                           │
│  │  (immediate/queued/     │◄── Trigger Evaluation                         │
│  │   scheduled/event)      │                                               │
│  └─────────────────────────┘                                               │
│       │                                                                     │
│       ├──► [If queued/scheduled/event] Wait Subscription                   │
│       │         │                                                           │
│       │         ▼                                                           │
│       │    Trigger Evaluation                                               │
│       │         │                                                           │
│       │         ▼                                                           │
│       │    Release Condition Met?                                           │
│       │         │                                                           │
│       │    ┌────┴────┐                                                      │
│       │    Yes      No                                                      │
│       │    ▼        ▼                                                       │
│       │ Continue  Wait                                                      │
│       │                                                                     │
│       ▼                                                                     │
│  ┌─────────────────────────┐                                               │
│  │  Memory Scope Confirm    │  Load: Short-term + Agent + Knowledge        │
│  └─────────────────────────┘                                               │
│       │                                                                     │
│       ▼                                                                     │
│  ┌─────────────────────────┐                                               │
│  │  Planning                │  Intent Resolver + Plan Builder               │
│  │  (Simple/Complex)        │                                               │
│  └─────────────────────────┘                                               │
│       │                                                                     │
│       ├──► Simple: Direct to Execution                                      │
│       │                                                                     │
│       └──► Complex: SubTask Splitting                                       │
│                   │                                                         │
│                   ▼                                                         │
│            ┌────────────────┐                                              │
│            │ Leader Agent   │  Parent Task                                   │
│            │ orchestration  │◄── Domain Agent Assignment                     │
│            └────────────────┘                                              │
│                   │                                                         │
│                   ▼                                                         │
│            ┌────────────────┐                                              │
│            │ SubTask Queue  │  Per-Domain Agent                             │
│            └────────────────┘                                              │
│                   │                                                         │
│                   ▼                                                         │
│  ┌─────────────────────────────────────────────────────────┐               │
│  │  Agent Execution (ReAct Loop)                           │               │
│  │                                                         │               │
│  │  ┌───────────┐    ┌───────────┐    ┌───────────┐       │               │
│  │  │ Reasoning │───►│  Action   │───►│Observation│       │               │
│  │  │ Summary   │    │(ToolCall)│    │ Summary   │       │               │
│  │  └───────────┘    └───────────┘    └───────────┘       │               │
│  │        │                                       │        │               │
│  │        └───────────────┬───────────────────────┘        │               │
│  │                        ▼                                 │               │
│  │                  Done?                                   │               │
│  │                  / Failed                                 │               │
│  └─────────────────────────────────────────────────────────┘               │
│       │                                                                     │
│       ▼                                                                     │
│  ┌─────────────────────────┐                                               │
│  │  Output Generation      │  Output Service                                │
│  │  (Final/Arrangement)    │                                               │
│  └─────────────────────────┘                                               │
│       │                                                                     │
│       ▼                                                                     │
│  ┌─────────────────────────┐                                               │
│  │  Event Persisted        │  Tracking & Event Service                     │
│  │  + WebSocket Push       │                                               │
│  └─────────────────────────┘                                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 13.5 部署目录结构

```
biosbot/
├── config/
│   ├── default.json          # 默认配置
│   ├── development.json      # 开发环境
│   └── production.json      # 生产环境
├── src/
│   ├── main.ts               # 入口文件
│   ├── app.ts                # 应用实例
│   ├── routes/               # 路由定义
│   ├── middleware/           # 中间件
│   ├── services/             # 服务层
│   │   ├── application/      # 应用层
│   │   ├── orchestration/    # 编排层
│   │   ├── domain/          # 领域层
│   │   └── infrastructure/  # 基础设施层
│   ├── models/               # 数据模型
│   ├── entities/             # 实体定义
│   ├── utils/                # 工具函数
│   └── types/                # 类型定义
├── workspace/                # 工作区
│   ├── uploads/              # 上传文件
│   ├── outputs/              # 生成文件
│   └── cache/                # 缓存
├── data/
│   ├── database.sqlite       # SQLite数据库
│   ├── knowledge.lancedb     # 向量知识库
│   └── logs/                 # 日志
├── skills/                   # Skill定义
│   ├── skill-1/
│   │   ├── SKILL.md
│   │   └── scripts/
│   └── ...
├── package.json
└── README.md
```

## 14. 概要设计结论

概要设计的关键不是“更多模块”，而是“明确边界”。BiosBot 必须保持以下单一原则：

- 一个主中心：Task。
- 一条主链路：输入 -> 入口判定与等待 -> 编排 -> 执行 -> 输出 -> 追踪。
- 一套统一语义：Task、TaskStep、Attachment、TaskOutput、ExecutionEvent。